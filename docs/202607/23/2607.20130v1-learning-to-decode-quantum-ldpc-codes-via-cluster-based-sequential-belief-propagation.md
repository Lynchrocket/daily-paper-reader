---
title: Learning to Decode Quantum LDPC Codes via Cluster-Based Sequential Belief Propagation
title_zh: 通过基于聚类的顺序置信传播学习解码量子LDPC码
authors: "Mohsen Moradi, Taejoon Kim, Rémi A. Chou, David G. M. Mitchell"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20130v1"
tags: ["query:qec-decoding"]
score: 9.0
evidence: 提出基于聚类的顺序置信传播解码量子LDPC码
tldr: 量子LDPC码的BP解码受短环和简并限制，强化学习顺序调度可改善性能但并行性有限。本文提出基于聚类的顺序调度，将变量节点分组为簇，RL代理选择簇后并行更新，并引入置换不变直方图状态表示。该方法在保持误码率性能的同时大幅减少调度决策次数，实现延迟与并行性的折中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20130v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 902, \"height\": 632, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20130v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 936, \"height\": 630, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20130v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 939, \"height\": 629, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20130v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 938, \"height\": 561, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20130v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 937, \"height\": 731, \"label\": \"Figure\"}]"
motivation: 现有变量节点级顺序调度并行性差，聚类方法可在保持性能下提升吞吐量。
method: 将变量节点划分为固定簇，RL代理选择簇后并行更新簇内节点，采用归一化失重直方图量化作为状态。
result: 与变量节点级调度相比，误码率性能几乎不变，但每次迭代调度决策数显著减少。
conclusion: 聚类顺序调度为量子LDPC码BP解码提供了高效延迟-并行性权衡方案。
---

## 摘要
基于置信传播（BP）的量子低密度奇偶校验（QLDPC）码解码因其低复杂度而具有吸引力，但其性能通常受到短环、简并性和收敛失败的限制。最近，基于强化学习的顺序变量节点（VN）调度（RL-S）通过学习状态相关的更新顺序改进了BP解码。然而，该方法逐VN处理的特性限制了迭代内的并行性，因为一次只更新一个VN。在本文中，我们提出了一种基于聚类的RL-S扩展方法用于QLDPC码。VN被划分为固定簇，在每个调度步骤中，RL智能体选择一个簇进行更新，之后该簇中的所有VN使用相同的更新前传入消息并行更新。为了使表格式状态空间对大簇大小保持实用，我们引入了基于局部失配权重归一化直方图后进行量化的排列不变簇状态。这种表示使得簇状态的数量取决于量化分辨率而非簇大小。我们还开发了相应的簇级马尔可夫决策过程、奖励函数和Q学习更新。在代表性QLDPC码上的数值结果表明，所提出的聚类学习调度保留了VN级顺序学习调度的大部分误码率优势，同时显著减少了每次BP迭代的调度决策次数，从而提供了有吸引力的延迟-并行性权衡。

## Abstract
Belief-propagation (BP) decoding for quantum low-density parity-check (QLDPC) codes is attractive due to its low complexity, but its performance is often limited by short cycles, degeneracy, and convergence failures. Recently, reinforcement-learning-based sequential variable-node (VN) scheduling (RL-S) was shown to improve BP decoding by learning state-dependent update orders. However, the VN-by-VN nature of that approach offers limited within-iteration parallelism, since only one VN is updated at a time. In this paper, we propose a cluster-based extension of RL-S for QLDPC codes. The VNs are partitioned into fixed clusters, and at each scheduling step the RL agent selects one cluster to update, after which all VNs in that cluster are updated in parallel using the same pre-update incoming messages. To keep the tabular state space practical for large cluster sizes, we introduce a permutation-invariant cluster state based on a normalized histogram of local mismatch weights, followed by quantization. This representation makes the number of cluster states depend on the quantization resolution rather than the cluster size. We also develop the corresponding cluster-level Markov decision process, reward function, and Q-learning update. Numerical results on representative QLDPC codes show that the proposed clustered learned scheduling preserves most of the error-rate benefit of VN-level learned sequential scheduling while substantially reducing the number of scheduling decisions per BP iteration, thereby providing an attractive latency-parallelism tradeoff.

---

## 论文详细总结（自动生成）

# 论文总结：Learning to Decode Quantum LDPC Codes via Cluster-Based Sequential Belief Propagation

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：量子低密度奇偶校验（QLDPC）码的置信传播（BP）解码受限于Tanner图中的大量短环和量子简并（degeneracy），导致收敛失败或错误性能差。
- **已有改进**：强化学习顺序变量节点（VN）调度（RL-S）通过学习状态依赖的更新顺序显著提升了BP解码性能，但其逐VN更新的方式导致迭代内并行性极差，串行调度深度与码长成正比，不利于低延迟实现。
- **本文目标**：提出基于聚类的顺序调度（Clustered RL-S），在保留RL-S性能优势的同时大幅减少调度决策次数，实现延迟与并行性的折中。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将全部VN划分为固定大小的不相交簇（cluster），强化学习智能体在每个调度步骤选择整个簇进行更新，簇内所有VN并行执行BP消息传递和硬判决更新。
- **关键技术细节**：
  - **簇划分**：随机均匀划分，簇大小B固定，最后簇可能较小；划分一次训练/测试中不变。
  - **簇状态表示**：
    - 每个VN定义局部失配权重ω_i = 其相邻未满足校验节点个数。
    - 对簇内所有VN的ω_i统计直方图并归一化，再通过最大余数法量化为长度为L的整数向量σ_a。
    - 该表示具有排列不变性，且状态空间大小由量化分辨率L而非簇大小B决定（状态数 = C(L+Amax, Amax)），使得Q表 size 可控。
  - **簇级MDP**：
    - 动作：选择未访问的簇。
    - 奖励：归一化失配减少量 r = (w_before - w_after) / Σ(邻接度)，若达成零失配加+1。
    - Q学习更新：使用一步前瞻值V_{t+1}，标准Q更新公式。
  - **并行簇更新**：簇内所有VN先基于更新前消息计算新硬判决，然后同时应用所有翻转。失配更新遵循奇偶性规则：校验节点状态翻转当且仅当其连接的翻转VN数为奇数。
- **算法流程**（文字描述）：
  - 训练：ϵ-贪心选择簇，执行并行更新，计算奖励，更新Q表。
  - 推理：每次贪心选择当前Q值最大的簇，直到失配为零或迭代结束。

## 3. 实验设计
- **数据集/信道模型**：
  - 独立Pauli‑X信道（仅比特翻转），使用码字 [[882,24,18≤d≤24]] 代码B1。
  - 去极化信道（depolarizing channel），使用 [[288,12,18]] 双变量自行车（BB）码。
- **训练设置**：物理误差概率从 Ptrain = {0.03,0.04,0.05,0.06,0.07} 均匀采样，最大迭代 Imax = 100（训练时）。
- **对比方法**：
  - 常规BP（QBP）
  - QBP+引导消减（QBPGD）
  - VN级RL‑S（无聚类，B=1）
- **评估指标**：块错误率（BLER），考虑逻辑错误和未收敛。
- **参数变化**：
  - 簇大小B（1,10,20,40,50,60,80,100）
  - 量化分辨率L（2,5,8,10,16,32,42 等）
  - 迭代预算T（10,100,1000,10000）
- **实验组数**：主要包括图2~图5共四组主要对比。图2：不同B在B1码上的BLER；图3：B=60与VN级RL-S在不同T下的比较；图4：不同B和L的组合效果；图5：去极化信道下比较QBP、QBPGD、RL‑S和聚类RL‑S。

## 4. 资源与算力
- **文中未明确说明**使用的GPU型号、数量、训练时长、内存等硬件资源。
- 仅提及训练采用ϵ-greedy策略，Q‑learning超参数（学习率α、折扣因子γ）未给出具体数值，只提到α∈(0,1], γ∈[0,1)。
- 未提供训练轮数Emax的实际取值。

## 5. 实验数量与充分性
- **数量**：4幅主要图表，涵盖两种信道、两个不同码字、多种簇尺寸B、多种量化分辨率L、多种迭代预算T、与多个基线（BP、QBPGD、VN级RL‑S）对比。
- **充分性**：
  - 优点：关键参数（B、L、T）均做了系统消融，验证了性能-延迟权衡；量化分辨率L的效果有专门实验；去极化信道下的结果验证了扩展性。
  - 不足：仅测试了两个具体的QLDPC码，泛化性证据有限；未与最新后处理型解码器（如BP‑OSD、BP‑SI、check‑agnosia等）直接对比；未提供训练收敛曲线或训练时间统计。
- **公平性**：所有对比在相同迭代上限或较大预算下进行，方法间比较合理。

## 6. 主要结论与发现
- **性能保持**：当聚类大小B≤60时，聚类RL‑S的BLER与VN级RL‑S几乎相同，在迭代预算较大（T≥100）时尤为接近。
- **延迟降低**：对于[[882,24,…]]码，B=60时每迭代仅需15次调度决策（原需882次），串行深度降低约59倍。
- **状态表示有效性**：量化直方图状态（L=8~42）能以紧凑形式保留足够信息，性能损失很小。
- **去极化信道**：聚类RL‑S同样有效，当L=10时性能接近VN级RL‑S，且优于QBP和QBPGD。
- **整体**：聚类学习调度提供了吸引力强的延迟‑并行性折中，适合硬件实现。

## 7. 优点
- **创新性**：首次将聚类思想引入学习型顺序BP调度，解决并行性瓶颈，方法原创性强。
- **状态设计巧妙**：直方图+量化实现排列不变性和状态空间压缩，使Q表规模可控，不随簇大小爆炸。
- **理论支撑**：提供并行簇更新的失配变化引理（Lemma 1），确保更新规则自洽。
- **实验系统**：对B、L、T等都进行了独立影响测试，结果清晰展示了权衡曲线。

## 8. 不足与局限
- **实验覆盖有限**：仅测试两个码字，且未包含其他常见QLDPC构造（如超图乘积码、均衡乘积码、Tanner码等）；未在更广泛的错误率范围内验证（高错误率区域未充分展示）。
- **硬件假设理想化**：随机簇划分未考虑冲突避免，实际硬件中可能引入额外延迟；理想并行更新模型可能与真实硅实现有差距。
- **对比基线不全面**：未与当前最先进的混合解码器（如BP‑OSD、BP‑SI、解码器‑M等）比较，不足以证明该方法相对于后处理方法的优势。
- **训练细节缺失**：超参数（α, γ, ϵ调度、Emax）未列出，导致可重复性存疑；未报告训练计算量。
- **应用限制**：仅适用于量子LDPC码的有限类（CSS码），独立Pauli‑X和depolarizing信道，实际电路级噪声下的表现未知。
- **未讨论多轮迭代间的聚类重访问策略**：当前每迭代每个簇只访问一次，可能限制性能进一步提升。

（完）
