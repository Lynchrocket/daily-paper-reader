---
title: Best-First Ordered Statistics Decoding of Quantum LDPC Codes
title_zh: 量子LDPC码的最佳优先有序统计译码
authors: "Michele Banfi, Marco Ferrari, Antonino Favano, Alberto Tarable, Luca Barletta"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25777v1"
tags: ["query:qec-decoding"]
score: 9.0
evidence: 量子LDPC码的解码算法
tldr: 量子LDPC码的BP+OSD解码因码退化需大量枚举，复杂度高且在全电路级噪声下BP不可靠。本文提出Best-First OSD (BF-OSD)，按似然递减顺序高效遍历错误候选，并在固定少量BP迭代后触发OSD。在Bivariate Bicycle码上，BF-OSD以1/100的查询预算达到与BP+OSD基线相同的性能，显著降低解码复杂度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25777v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 930, \"height\": 598, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25777v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 772, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25777v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 274, \"label\": \"Table\"}]"
motivation: 现有BP+OSD解码复杂度高且在全电路级噪声下BP不可靠，需更高效的候选空间搜索方法。
method: 提出BF-OSD，按错误候选的似然递减顺序搜索，并在固定少量BP迭代后启动OSD，避免等待BP收敛。
result: 在BB码全电路级噪声模拟中，BF-OSD以BP+OSD的1/100查询预算达到相同性能。
conclusion: BF-OSD有效降低了量子LDPC解码复杂度，适用于噪声条件下的实际解码。
---

## 摘要
置信传播（BP）后接有序统计译码（OSD）已成为量子低密度奇偶校验（QLDPC）码译码的黄金标准。该领域的最新进展提出了新的方法和算法，以降低这一标准流程的复杂度。由于码的退化性，更一般地，因为多个不同的错误模式可以产生相同的综合征，OSD本质上是一种列表译码技术；即它枚举一组综合征一致的候选者，并返回最有可能的一个。在这项工作中，我们提出了一种OSD的变体，称为最佳优先OSD（BF-OSD），它通过按似然递减的顺序遍历错误候选空间，而不是通过暴力枚举预选子集，从而更高效地搜索错误候选空间。此外，我们摒弃了传统的BP+OSD级联方式：不是根据BP收敛性来触发OSD，而是在固定的小次数BP迭代后调用OSD。这一设计选择是由于全电路级噪声环境下BP特别不可靠。在全电路级噪声下对一系列二元自行车（BB）码的蒙特卡洛仿真表明，BF-OSD在性能上与BP+OSD基准相当，但查询预算仅为后者的1/100。

## Abstract
Belief Propagation (BP) followed by Ordered Statistics Decoding (OSD) has emerged as the gold standard for decoding quantum low-density parity-check (QLDPC) codes. Recent advancements in this field have proposed new methods and algorithms to lower the complexity of this standard pipeline. Because of code degeneracy, and more in general because multiple distinct error patterns can produce the same syndrome, OSD is inherently a list-decoding technique; that is, it enumerates a set of syndrome-consistent candidates and returns the most probable one. In this work, we propose a variant of OSD, which we call Best-First OSD (BF-OSD), that explores the error-candidate space more efficiently by traversing it in order of decreasing likelihood, rather than by brute-force enumeration of a pre-selected subset. In addition, we depart from the conventional BP+OSD cascade: instead of conditioning the OSD invocation on BP convergence, we invoke OSD after a fixed, small number of BP iterations. This design choice is motivated by the full circuit-level noise regime, in which BP is particularly unreliable. Monte Carlo simulations of a family of Bivariate Bicycle (BB) codes under full circuit-level noise show that BF-OSD matches the performance of the BP+OSD baseline while exploring the solution space with 1/100th of the query budget.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：当前量子LDPC码的黄金标准解码器是置信传播（BP）后接有序统计译码（OSD）。然而，标准OSD（如OSD-w和OSD-CS）在枚举错误候选时效率低下——它仅对预选的固定子集进行暴力枚举，而非按照候选的似然顺序搜索，导致大量计算浪费。同时，在全电路级噪声模型下（考虑CNOT门错误传播、多次综合征提取等），解码矩阵变得稠密且短环众多，BP收敛缓慢且不可靠，传统的“BP失败后再调用OSD”策略在此场景下效率极低。
- **核心问题**：如何设计一种低复杂度（尤其是低查询预算）的OSD变体，使其在全电路级噪声下仍能保持接近最大似然（ML）译码的性能，同时避免对BP收敛的依赖。
- **整体含义**：通过改变候选空间的遍历顺序和简化BP迭代，大幅降低解码复杂度（查询预算降至1/100），使得量子LDPC码的实际硬件解码变得更加可行。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将OSD的候选搜索视为对零空间仿射余集的遍历，利用最佳优先搜索（best-first search）按成本（即负对数似然）递增顺序评估候选，确保在固定查询预算内始终获得当前最优解。
- **关键技术细节**：
    - **BF-OSD算法**：
        1. 基于置信度排序的OSD-0：采用Fossorier和Lin的经典排序（按LLR绝对值升序），并先对所有LLR为负的比特进行预翻转（吸收到综合征中），使所有LLR非负。这使得最不可靠的比特成为枢轴（pivot），最可靠的比特成为自由（free）变量，得到基线候选 `E_base`。
        2. 构造零空间生成器 `g_j`：对每个自由列 `j`，定义生成向量（仅在自由列本身和与枢轴行相关的位置为1）。
        3. 定义每个生成器的权重 `w_j`：即单独异或该生成器到基线候选时增加的成本（由翻转比特的LLR之和决定）。
        4. 最佳优先遍历：从空集（对应于 `E_base`）开始，使用优先队列按候选总成本排序。扩展节点时，只允许添加索引大于当前最大索引的生成器，防止重复。子节点成本通过增量更新（只计算被翻转比特的LLR变化）。
        5. 在固定查询预算 `Q` 内不断弹出最小成本候选并记录当前最佳，最终返回最低成本候选。
    - **BP调度的修改**：不再等待BP收敛，而是仅运行少量迭代（远小于传统10000次）并采用串行更新调度（sequential schedule）以加速局部信息传播。即使BP未收敛，其产生的非均匀LLR也足够为BF-OSD提供指导。
    - **列排序的影响分析**：详细比较了两种排序（LLR排序 vs 置信度排序），论证了本文采用的置信度排序（预翻转+绝对值升序）在BP少量迭代场景下的优势，避免将不确定的比特强制赋零或将置信错误的比特强制设为枢轴。

## 3. 实验设计：使用了哪些数据集/场景，benchmark，对比方法
- **数据集/场景**：采用文献[4]中报告的五种Bivariate Bicycle（BB）码：[[72,12,6]], [[90,8,10]], [[108,8,10]], [[144,12,12]], [[288,12,18]]。噪声模型为全电路级噪声，模拟 `Nc = d` 轮综合征提取（d为码距），物理错误率 `p = 10^{-3}`（主对比点），以及从低到高的完整物理错误率范围（仅展示曲线图）。
- **Benchmark**：使用文献[4]中的标准BP+OSD-CS解码器（λ=7）作为基线，该基线运行BP直到收敛（最多10000次迭代）或失败后调用OSD。
- **对比方法**：本文提出的BP+BF-OSD（固定少量BP迭代+BF-OSD），查询预算Q与基线可比（原始预算和1%预算两种情况）。另外内部测试了两种列排序（LLR排序 vs 置信度排序）的效果。

## 4. 资源与算力
- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长等硬件资源信息。仅给出了算法复杂度分析（BP: O(IBP|E|), OSD-0: O(N log N + r^2 N), 候选搜索：BF-OSD O(Q(N + log Q))）。实验部分仅说明使用蒙特卡洛仿真，未描述计算环境。因此无法评估实际算力开销。

## 5. 实验数量与充分性
- **实验组数**：主要在五种BB码上进行了对比，给出了一个核心表格（p=10^-3下的pL值）和一个完整物理错误率范围的曲线图（图2）。此外，还测试了查询预算降至1%时的性能（结果类似），以及对两种列排序进行了对比（未发现统计显著差异）。
- **充分性评估**：
    - **优势**：覆盖了从d=6到d=18的不同距离码，展示了阈值以下刻度行为；提供了与基线直接公平的对比（相同仿真器、相同噪声模型）。
    - **不足**：
        - 仅测试了BB码一族，未在其他QLDPC码（如超图积码、散列码）上验证。
        - 未进行正式的消融实验（如单独分析BP迭代次数影响、排序策略在不同Q下的详细对比、min-sum近似下的表现等）。
        - 仅在单一物理错误率（p=10^-3）下给出数值表格，其他点仅通过曲线呈现，缺乏量化差异。
        - “查询预算降至1%结果类似”未给出具体数值，描述略显模糊。
    - 总体而言，实验设计聚焦于性能匹配和效率验证，但泛化性和细化分析不足。

## 6. 论文的主要结论与发现
- BF-OSD在五个BB码的全电路级噪声仿真中，与BP+OSD-CS基线达到了几乎相同的每轮逻辑错误率（pL），但查询预算仅需基线的1%即可实现同等性能（原始Q约为OSD-CS的 |D_CS| ≈ k + C(λ,2) 同量级；1%时保持性能）。
- 采用固定少量BP迭代+串行调度，避免等待BP收敛，在噪声场景下更高效。
- BF-OSD天然具有“任意时刻（anytime）”特性：可随时中断并返回当前最优候选，且成本单调递减。
- 在大查询极限下，BF-OSD等价于余集最大似然译码。
- 两种列排序在本文实验设置下未产生统计显著差异；但作者理论上认为置信度排序在BP未收敛时更优。

## 7. 优点
- **方法创新**：首次将最佳优先搜索引入OSD，实现了按似然顺序遍历候选，绕过传统OSD的固定子集枚举，效率极高。
- **复杂度优势**：在保持性能的前提下，将查询预算降低两个数量级，同时BP迭代次数大幅减少。
- **理论证明**：附录证明了基于贪婪算法选择的OSD-0枢轴集是最小成本基（matroid性质），为算法高效性提供了理论支撑。
- **系统性考量**：深入分析了列排序的影响并提出改进方案；重新设计BP+OSD触发策略以适应实际噪声模型。
- **实用导向**：算法支持任意时刻中断，适合实时解码需求。

## 8. 不足与局限
- **实验覆盖有限**：仅测试BB码，且仅在一种硬件噪声模型（全电路级，源自[4]）下仿真。未在代码容量噪声、各种偏置噪声或不同码族上进行验证，泛化能力未确认。
- **缺乏消融实验**：未系统分析BP迭代次数、串行调度 vs 泛洪调度、排序策略等各组件对最终性能的独立影响；未对比不同Q选择（仅汇报1% Q的数据，但未给出Q=1%、10%、50%的完整曲线）。
- **潜在偏差风险**：仿真数据集仅来自单一文献的固定设置，可能有数据依赖性。基线使用BP+OSD-CS，但BF-OSD也采用了相同的O-0基础，对比可能存在未充分控制的变量（如OSD-CS的λ=7是否最优？BF-OSD的Q如何选取才能公平？）。
- **应用限制**：算法依赖于从BP获得的LLR质量，在BP完全无法提供有用软信息的极端噪声下（如p远高于阈值），BF-OSD可能退化。此外，BF-OSD的优先队列实现需要 log Q 的额外开销，在超大规模码（如N>10^4）上可能成为瓶颈，论文未讨论。
- **硬件实现考量**：未评估实际硬件上的延迟和并行化潜力；也指出当前未在min-sum近似下测试，而min-sum是硬件友好实现的关键。

（完）
