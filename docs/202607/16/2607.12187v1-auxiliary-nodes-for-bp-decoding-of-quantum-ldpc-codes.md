---
title: Auxiliary Nodes for BP Decoding of Quantum LDPC Codes
title_zh: 量子LDPC码BP解码的辅助节点
authors: "Daniel Tandler, Paul Bezner, Stephan ten Brink"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.12187v1"
tags: ["query:qec-decoding"]
score: 9.0
evidence: QLDPC码的BP解码辅助节点
tldr: 量子LDPC码的BP解码受短环和简并性影响性能。本文提出在解码图中引入辅助变量节点和辅助校验节点的通用框架，兼容标准稳定子测量，增加设计自由度。该框架可统一解释4环移除和子码集成解码，并进一步提出图导出子码集成解码器。在电路级噪声下，该方法相比无4环解码图的BP显著降低每轮逻辑错误率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12187v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 888, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12187v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 683, \"height\": 384, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12187v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 900, \"height\": 1164, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12187v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 951, \"height\": 704, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12187v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 935, \"height\": 1536, \"label\": \"Figure\"}]"
motivation: 量子LDPC码的BP解码因短环和简并性性能不佳，需要新的解码图设计自由度来改善。
method: 提出在CSS码解码图中引入辅助变量节点和辅助校验节点的通用框架，并基于该框架设计图导出子码集成解码器。
result: 在电路级噪声下，图导出子码集成解码器相比无4环解码图的BP显著降低每轮逻辑错误率。
conclusion: 辅助节点框架为提升量子LDPC码BP解码性能提供了新途径，可扩展至其他解码优化。
---

## 摘要
许多最近提出的Calderbank-Shor-Steane（CSS）量子低密度奇偶校验（QLDPC）码具有稀疏的解码图，使得基于综合征的置信传播（BP）解码可以实现低复杂度。然而，它们的构造常常导致损害BP性能的特性，例如短环和退化性。在这项工作中，我们提出了一个通用框架，用于在CSS码的解码图中引入辅助变量节点（AVN）和辅助校验节点（ACN），并与标准的稳定子测量框架兼容。这为解码图本身的设计提供了额外的自由度，可用于解决上述缺点。我们表明，最近提出的技术，即4环移除和子码集成解码，可以解释为该框架的实例。对于4环移除，我们发现增益强烈依赖于BP迭代次数和校验节点消息缩放。基于此框架，我们进一步提出了一种图派生的子码集成解码器，并在电路级噪声下证明，与相应的无4环解码图上的BP相比，它显著降低了每轮逻辑错误率。

## Abstract
Many recently proposed Calderbank-Shor-Steane (CSS) quantum low-density parity-check (QLDPC) codes have sparse decoding graphs, enabling syndrome-based belief propagation (BP) decoding at low complexity. Their construction, however, often results in properties that impair BP performance, such as short cycles and degeneracy. In this work, we propose a general framework for introducing auxiliary variable nodes (AVNs) and auxiliary check nodes (ACNs) into the decoding graph of CSS codes, compatible with the standard stabilizer measurement framework. This provides an additional degree of freedom in the design of the decoding graph itself and can be used to tackle the aforementioned shortcomings. We show that recently proposed techniques, 4-cycle removal and subcode ensemble decoding, can be interpreted as instances of this framework. For 4-cycle removal, we find that the gains depend strongly on the BP iteration count and check-node message scaling. Building on this framework, we further propose a graph-derived subcode ensemble decoder and demonstrate under circuit-level noise that it substantially reduces the per-round logical error rate compared with BP on the corresponding 4-cycle-free decoding graph.

---

## 论文详细总结（自动生成）

# 中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：量子低密度奇偶校验（QLDPC）码的置信传播（BP）解码性能受两个关键因素影响：解码图中的短环（尤其是4-环）导致消息相关性与振荡；量子特有的退化性（不同错误假设产生相同综合征）使BP难以收敛。
- **已有改进的局限**：现有方法如后处理（OSD）、多轮多样化解码、直接修改解码图（如GARI）等，或增加延迟，或适用范围有限，且缺乏统一理论视角。
- **本文目标**：提出一个通用框架，通过在解码图中引入**辅助变量节点（AVN）** 和**辅助校验节点（ACN）**，在保持原始解码问题等价的前提下增加设计自由度，从而同时缓解短环和退化问题，并统一解释4-环移除与子码集成解码。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将原始CSS码的奇偶校验矩阵 \( H \) 扩展为 \( H_{\text{aug}} = [H_{\text{sys}} \mid M] \)，其中 \( M \) 对应AVN的列，同时追加新行（ACN）和相应的syndrome映射 \( s_{\text{aug}} = A s \)。该扩展通过线性代数变换（Lemma 1, 2, Proposition 1）保证解空间与原问题一一对应，因此可直接在扩展图上运行标准BP解码。
- **关键技术细节**：
  - **AVN插入**：在 \( H \) 右侧添加全零列，对应了一个度为1的AVN。
  - **ACN插入**：添加新行 \( c_{\text{aug}}^T = (c'^T, 1) \)，其中 \( c' \) 可任意选择。
  - **syndrome映射**：利用高斯消元得到矩阵 \( A \) 使得 \( H_{\text{map}} = A H \)，从而 \( s_{\text{aug}} = A s \) 可由原始syndrome计算得出（Algorithm 1）。
  - **4-环移除（Algorithm 2）**：源自经典编码的优化4-环移除方法，通过选择最大4-环块，优先尝试行加法（case 1/2），否则引入AVN+ACN。
  - **图派生子码集成解码**：将4-环移除过程中生成的每个新行（其 \( c'^T \) 部分通常落在 \( \text{Row}(H_{\text{DEM}}) + \text{Row}(L) \) 之外）作为候选**退化分裂器**。集成解码有两种策略：
    - **静态集成**：使用最大覆盖启发式，从候选分裂器中选出一组使总体错误率最低的成员（每个成员对应两个AVN假设值0/1）。
    - **自适应集成**：先对4-环自由图运行BP，若未收敛则根据LLR幅度选出最不可靠的AVN，以其对应ACN构建集成。

## 3. 实验设计
- **噪声模型与场景**：采用电路级噪声模型，通过探测器错误模型（DEM）描述。默认物理错误率 \( p = 0.001 \)，重复测量轮数 \( r = d \)（代码最小距离）。实验使用QUITS仿真框架。
- **数据集（代码）**：主要测试两个BB（bivariate bicycle）码：
  - \( [\![72, 12, 6]\!] \) 码
  - \( [\![90, 8, 10]\!] \) 码
- **对比方法**：
  - 标准BP（在原始DEM矩阵 \( H_{\text{DEM}} \) 上）
  - BP（在4-环自由矩阵 \( H_{\text{free}} \) 上）
  - BP + OSD-0（经典后处理器）
  - 本文提出的自适应与静态集成解码
- **评估指标**：每轮逻辑错误率 \( p_L \)（从逻辑错误率LER按 \( p_L = 1 - (1 - \text{LER})^{1/r} \) 计算）。每个数据点至少收集200个逻辑错误事件。

## 4. 资源与算力
- **未明确说明**：论文未提及具体使用的GPU型号、数量、训练时长或CPU核心数。仅提到仿真基于QUITS框架和开源BP解码器实现，所有实验在标准计算机上完成。因此无法量化算力消耗。

## 5. 实验数量与充分性
- **实验组数**：
  - **4-环移除参数依赖**：在15个中间检查点测量4-环数、边数、\( p_L \) 随不同BP迭代次数 \( I \in \{5,20,100\} \) 和缩放因子 \( \alpha \in \{0.8,0.9,1.0,1.1,1.2\} \) 的变化。——共约15×5=75组，但图中只展示了部分结果。
  - **集成消融（图3）**：静态集成 \( E \) 从1到13；自适应集成在 \( E \) 和 \( T \)（先验步骤）不同组合下形成曲面（约20个点）。——每组至少200个逻辑错误事件。
  - **最终性能对比（图4）**：两个代码，各6–8个物理错误率点，对比4种方法。——共约48组（含重复）。
- **充分性**：
  - 覆盖了核心参数（迭代次数、缩放因子、集成大小、初始BP步数），消融研究较完整。
  - 但仅测试了两类BB码，未涉及其他QLDPC码族（如双曲或广义Bicycle码）；噪声范围仅在 \( 10^{-3} \) 到 \( 6 \times 10^{-3} \) 之间，未考察更高或更低错误率区域。
  - 对比方法有限，未与最新神经网络解码器、多轮多样化解码（如VIBE）等方法比较。
  - 整体实验设计客观（固定随机种子、充足采样），但泛化性证据不足。

## 6. 论文的主要结论与发现
- **4-环移除效果**：移除4-环总体上可降低BP逻辑错误率，但其增益高度依赖于BP迭代次数和校验节点消息缩放因子 \( \alpha \)；迭代次数少时性能甚至恶化（因零先验AVN扩大图）。
- **子码集成解码**：利用4-环移除产生的辅助节点构建集成，显著优于仅在4-环自由图上的BP；对于\( [\![72,12,6]\!] \)码，静态集成（\( E=12 \)）几乎追上BP+OSD-0的性能，且可并行化维持低延迟。
- **自适应与静态比较**：在多数设置下静态集成优于自适应（基于LLR排序），表明AVN排序策略需要进一步优化。
- **对大代码的挑战**：对\( [\![90,8,10]\!] \)码，即使集成128个成员仍有较大性能差距，说明大代码可能需要窗口化或分层解码等补充技术。

## 7. 优点
- **理论统一性**：提出的AVN/ACN框架以简洁的代数形式统一了图增强、4-环移除和子码集成，并严格证明了解码问题等价性。
- **创新性**：首次将4-环移除过程中产生的辅助节点重新用作退化分裂器，无需额外开销；静态集成提供稳定性能，且易于并行化（适合实时量子纠错）。
- **兼容性**：框架与现有标准稳定子测量架构完全兼容（不需要额外测量或修改量子电路），可直接集成到现有解码流水线。
- **实验设计仔细**：对不同BP参数做了系统消融，揭示了4-环移除的敏感性，有助于指导实际应用中的调优。

## 8. 不足与局限
- **实验覆盖有限**：
  - 仅测试两个BB码，未验证在更大规模的码（如[[144,12,12]]）或不同拓扑（如超图、曲面码）上的表现。
  - 电路级噪声模型仅设定单一物理噪声模型（均匀错误概率），未考虑各向异性或相关错误。
  - 对比基线不够全面：缺少与近年提出的超图集成解码、预解码分裂（SymBreak）、基于神经网络的BP等方法的比较。
- **自适应策略缺陷**：基于LLR幅度选择AVN可能不是最优分裂器，论文承认“可能需要更精细选择”。实验显示静态更好，说明自适应设计需改进。
- **对大代码的性能瓶颈**：集成成员数量增大并未显著弥合与OSD的差距，提示该方法在高度退化代码上仍显不足，需结合窗口化或分层解码。
- **复杂度与延迟**：虽然文中提及可并行性，但未量化集成解码相比单一解码的复杂度增加（边数、迭代次数、并行开销），也未与OSD相比的延迟/资源权衡分析。
- **依赖参数调优**：4-环移除及集成解码均涉及超参数（迭代次数、缩放因子、集成大小等），最佳设置可能因代码而异，缺乏鲁棒的自动调参机制。

（完）
