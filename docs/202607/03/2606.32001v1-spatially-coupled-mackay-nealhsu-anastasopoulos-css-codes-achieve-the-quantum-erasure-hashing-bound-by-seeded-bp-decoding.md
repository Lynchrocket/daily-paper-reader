---
title: Spatially Coupled MacKay-Neal/Hsu-Anastasopoulos CSS Codes Achieve the Quantum-Erasure Hashing Bound by Seeded BP Decoding
title_zh: 空间耦合的MacKay-Neal/Hsu-Anastasopoulos CSS码通过种子BP解码达到量子擦除哈希限
authors: Kenta Kasai
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.32001v1"
tags: ["query:qec-decoding"]
score: 9.0
evidence: 通过种子BP解码的空间耦合CSS码，在量子擦除信道上达到散列界
tldr: 经典稀疏图编码中，空间耦合使BP解码达到未耦合系统的MAP阈值。本文在量子擦除信道下，针对空间耦合MN/HA型CSS码，通过密度演化分析证明有限度因子图的种子BP解码能达到X/Z等速率设计速率对应的哈希界。该结果为量子纠错码的有限度实现提供了理论依据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.32001v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1540, \"height\": 1885}, {\"url\": \"assets/figures/arxiv/2606.32001v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1086, \"height\": 959}, {\"url\": \"assets/figures/arxiv/2606.32001v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1645, \"height\": 2000}, {\"url\": \"assets/figures/arxiv/2606.32001v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1411, \"height\": 1097}, {\"url\": \"assets/figures/arxiv/2606.32001v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1588, \"height\": 649}, {\"url\": \"assets/figures/arxiv/2606.32001v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1303, \"height\": 923}, {\"url\": \"assets/figures/arxiv/2606.32001v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1665, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2606.32001v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1665, \"height\": 285}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.32001v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1684, \"height\": 581}, {\"url\": \"assets/tables/arxiv/2606.32001v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1647, \"height\": 628}]"
motivation: 探究空间耦合MN/HA型CSS码在量子擦除信道上能否通过种子BP解码达到哈希界。
method: 定义稀疏校验矩阵的CSS系综，导出五消息非耦合密度演化递归，分解为Z边和X边分量，应用耦合向量势方法分析。
result: 种子BP解码的阈值达到X/Z等速率设计速率对应的哈希界。
conclusion: 在密度演化层面证明了有限度因子图的种子BP解码能实现量子擦除信道哈希界。
---

## 摘要
在经典稀疏图编码中，空间耦合是一种机制，使得置信传播（BP）解码能够达到未耦合系统的最大后验（MAP）或面积阈值性能。由于MacKay-Neal/Hsu-Anastasopoulos（MN/HA）打孔稀疏码集在MAP解码下达到容量，自然要问：空间耦合的MN/HA型Calderbank-Shor-Steane（CSS）码能否在量子擦除信道上通过种子BP解码达到哈希限？我们在硬擦除CSS解码的密度演化（DE）层面回答了这个问题。在擦除坐标上，两个二进制泡利分量仍未确定，等价地，擦除量子比特由四种泡利可能性表示。我们首先通过稀疏打孔矩阵和相应的稠密校验矩阵定义CSS码集。对于固定的Z侧、X侧和校验度数，然后推导出五消息未耦合DE递归，将其分解为Z侧和X侧组成系统，并定义两个组成势。分别对两个组成应用耦合向量势方法，证明了在所得有限度因子图上进行种子BP解码达到Z侧度数比和X侧互补度数比中的较小者。在X/Z等速率特化中，Z侧和X侧组成设计速率相等，此BP阈值是由设计速率确定的哈希限信道参数。因此，本文给出了一个DE层面的证明，表明有限度因子图的种子BP解码对于X/Z等速率族达到哈希限。有限长度BP集中性、误块收敛性以及理想DE种子的有限码实现是单独的问题。

## Abstract
In classical sparse-graph coding, spatial coupling is a mechanism by which belief-propagation (BP) decoding attains the maximum-a-posteriori (MAP) or area-threshold performance of the uncoupled system. Since MacKay-Neal/Hsu-Anastasopoulos (MN/HA) punctured sparse ensembles achieve capacity under MAP decoding, it is natural to ask whether spatially coupled MN/HA-type Calderbank-Shor-Steane (CSS) codes can reach the hashing bound on the quantum erasure channel under seeded BP decoding. We answer this question at the density evolution (DE) level for hard-erasure CSS decoding. On an erased coordinate, the two binary Pauli components remain unresolved, equivalently the erased qubit is represented by the four Pauli possibilities. We first define the CSS ensemble through sparse punctured matrices and the corresponding dense parity-check matrices. For fixed finite Z-side, X-side, and check degrees, we then derive a five-message uncoupled DE recursion, decompose it into Z-side and X-side constituent systems, and define the two constituent potentials. Applying the coupled-vector potential method to the two constituents separately proves that seeded BP decoding on the resulting finite-degree factor graphs reaches the smaller of the Z-side degree ratio and the X-side complementary degree ratio. In the X/Z equal-rate specialization, where the Z-side and X-side constituent design rates are equal, this BP threshold is the hashing-bound channel parameter determined by the design rate. Thus the paper gives a DE-level proof that seeded BP decoding with finite-degree factor graphs achieves the hashing bound for the X/Z equal-rate family. Finite-length BP concentration, block-error convergence, and a finite-code realization of the ideal DE seed are separate questions.

---

## 论文详细总结（自动生成）

# 论文总结：空间耦合的 MacKay-Neal/Hsu-Anastasopoulos CSS 码通过种子 BP 解码达到量子擦除哈希限

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：经典稀疏图编码中，空间耦合（spatial coupling）机制可使置信传播（BP）解码达到未耦合系统的最大后验（MAP）或面积阈值性能。MacKay-Neal/Hsu-Anastasopoulos（MN/HA）打孔稀疏码集在 MAP 解码下能达到信道容量。
- **核心问题**：将空间耦合思想应用于量子纠错领域，探究由 MN/HA 型打孔稀疏表示构造的 Calderbank-Shor-Steane（CSS）码，在量子擦除信道上能否通过种子 BP 解码达到哈希限（hashing bound）。
- **整体含义**：本文在密度演化（DE）层面给出了肯定的答案，证明了对于 X/Z 等速率设计率的有限度因子图，种子 BP 解码的阈值精确等于哈希限。这是实现有限度量子 LDPC 码达到容量性能的重要理论步骤。

## 2. 提出的方法论

### 核心思想
- 通过打孔稀疏表示构造 CSS 码集，将稠密的可见坐标校验矩阵嵌入稀疏的扩展矩阵中。
- 在量子擦除信道下，每个擦除量子比特对应两个未确定的二进制泡利分量，导出五消息（a, b, c, d, e）的硬擦除密度演化递归。
- 将五消息递归分解为 Z 侧和 X 侧两个独立的组成系统（每个系统类似于经典 BEC 上的 MN/HA 递归）。
- 对每个组成系统定义势函数（vector potential），应用耦合向量势方法证明阈值饱和。

### 关键技术细节
1. **码集定义**：
   - 定义 `(j,k,M)` 规则随机矩阵、`(j,k,M,L,w)` 尾咬合空间耦合规则随机矩阵。
   - 嵌套正则码集：`AZ`（jZ 度）、`AΔ`（jX-jZ 度）、`B`（k 度）构成扩展校验矩阵 `H'_Z` 和 `H'_X`，通过打孔隐藏变量得到可见码 `CZ`、`CX`。

2. **密度演化递归**：
   - 五消息：a（打孔变量→Z 侧 A 校验）、b（打孔变量→Z 侧 B 校验）、c（可见变量→Z 侧 B 校验）、d（X 侧辅助变量→X 侧校验）、e（X 侧可见变量→X 侧校验）。
   - 校验节点更新公式 (8)–(12)，变量节点更新公式 (13)，残差公式 (14)。
   - 分离得到 Z 侧系统 (15) 和 X 侧系统 (17)。

3. **向量势方法**：
   - 系统可容性条件（定义 5），定义势函数 `U(x;ϵ)`（式 19）。
   - 阈值定义：`ϵ_pot = sup{ϵ: ΔE(ϵ) > 0}`，其中 `ΔE(ϵ)` 是非成功不动点上的最小势。
   - 分别对 Z 侧和 X 侧构造势 `U_Z` (28) 和 `U_X` (31)。

4. **阈值饱和证明**：
   - 定理 6：在代数正性条件 `P_Z` 下，Z 侧势阈值为 `j_Z/k`。
   - 定理 7：在代数正性条件 `P_X` 下，X 侧势阈值为 `1 - j_X/k`。
   - 整体阈值 `ϵ_pot = min{j_Z/k, 1 - j_X/k}`（式 43）。
   - 在 X/Z 等速率条件 `j_Z + j_X = k` 下，`ϵ_pot = ϵ_hash = (1 - R_des_Q)/2`（定理 8）。
   - 定理 9：对每个组成系统，施加种子边界条件后，空间耦合 DE 饱和到 `ϵ_pot`。

5. **代数正性条件的可判定性**：
   - 定义剩余函数 R_Z (35) 和 R_X (36)，非负性条件 `P_Z`、`P_X` 可通过有限度数的 Sturm/resultant 多项式符号检验完全验证（命题 3）。

## 3. 实验设计

### 使用的“数据集”/场景
- 本文是理论分析论文，没有采用传统 benchmark 数据集。
- 数值验证在理论框架内进行，分为两部分：
  - **低度数穷举扫查**：枚举所有 `k ≤ 30` 的 X/Z 等速率三元组 `(j_Z, j_X, k) = (j, j+λ, 2j+λ)`，共 182 个三元组。对每个三元组，在 `[0.025 ϵ_hash, 0.975 ϵ_hash]` 区间内取 17 个 ϵ 值，求解 Z 侧和 X 侧组成固定点方程，检查非平凡固定点上的势是否为正。
  - **速率代表性扫查**：选取约 20 个目标速率 `r_i = i/21` 附近的低度三元组（最小化 k），`5 ≤ k ≤ 60`，验证势的正性。
  - **数值 DE 示例**：对 `(j_Z, j_X, k) = (4,8,12)`，取 `L=1024, w=16`，种子区间 `|S|=w`，`ϵ = 0.3325`（0.9975 ϵ_hash），迭代至收敛，展示 Z 侧和 X 侧残余随迭代下降至零。

### Benchmark 与对比方法
- 无对比方法。本文主要与理论预期的哈希限进行比较，而非与其他解码器性能对比。

## 4. 资源与算力
- **文中未明确说明使用的 GPU 型号、数量、训练时长等算力信息。**
- 数值 DE 示例和低度扫查通常可在普通 CPU 上完成，但论文未提供具体硬件或运行时间数据。

## 5. 实验数量与充分性
- **低度穷举扫查**：覆盖了所有 `k ≤ 30` 的 X/Z 等速率三元组（182 个），检测到 4495 个非平凡固定点，所有势最小值 > 0.209，没有发现反例。这为理论条件 `P_Z`、`P_X` 在低度范围内提供了强力数值支持。
- **速率代表性扫查**：约 20 个速率点覆盖了从低速率到高速率范围，未发现非平凡固定点（即势为空集），进一步验证了阈值预测。
- **数值 DE 示例**：仅一个参数配置，展示了 DE 收敛行为。
- **充分性评价**：实验设计符合理论论文的辅助验证角色，扫查覆盖全面、数值结果与理论一致。但缺乏与传统解码方法的对比实验，且在更高度数（k > 30）下未提供数据。主断言依赖于代数条件 `P_Z`、`P_X`，而对这些条件在一般度数下的完全验证仅给出了方法（Sturm/resultant），未给出全体度数对的正式证明。因此实验是充分的但非完全证明。

## 6. 主要结论与发现
- 对于固定有限度数的 Z 侧、X 侧和校验度数，在代数正性条件下：
  - 非耦合 DE 的势阈值为 `ϵ_pot = min{j_Z/k, 1 - j_X/k}`。
  - 种子空间耦合 DE 的 BP 阈值饱和到该值。
- 在 X/Z 等速率条件 `j_Z + j_X = k` 下：
  - `ϵ_pot = ϵ_hash = (1 - R_des_Q)/2`，即量子擦除哈希限参数。
- **核心结论**：在密度演化层面，有限度因子图的种子 BP 解码能够达到量子擦除哈希限。

## 7. 优点
- **理论创新**：将经典空间耦合阈值饱和理论成功推广到量子 CSS 码的擦除解码，处理了五消息耦合递归，并分解为两个更简单的组成系统。
- **方法普适性**：向量势方法为其他组合型递归的阈值分析提供了框架。
- **可验证性**：代数正性条件 `P_Z`、`P_X` 可转化为有限度数的多项式符号检验，使得度数的理论条件具有可计算性（尽管本文未给出所有度数对的完全形式证明）。
- **结构清晰**：论文组织严密，从码集定义、DE 推导、势函数构造到阈值饱和证明，逻辑链完整。

## 8. 不足与局限
- **局限性**：
  - **仅限于 DE 层面**：未包含有限长度集中性证明、误块概率收敛性分析，以及有限码的实际构造与仿真。从 DE 阈值到有限码性能仍有 gap。
  - **理想种子假设**：论文中的种子是 DE 边界条件（消息全零），在真实 CSS 码中如何实现等价的操作（如局部已知坐标）未给出具体方案。
  - **代数正性条件的直接验证**：论文仅以低度扫查作为数值证据，未对所有可能度数对给出严格数学证明。虽然提出了 Sturm/resultant 方法，但未实际应用于大范围度数。
  - **仅覆盖量子擦除信道和硬擦除解码**：未推广到一般噪声信道或软解码。
  - **未与其他量子解码方法比较**：无法说明该方法的实际性能优势。
- **潜在偏差**：数值扫查尽管覆盖了低度数范围，但可能存在未发现的特殊度数对使代数条件不成立，理论断言依赖于这些条件的假设。

（完）
