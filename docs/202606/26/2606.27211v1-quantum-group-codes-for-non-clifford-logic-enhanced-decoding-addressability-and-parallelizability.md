---
title: "Quantum group codes for non-Clifford logic: enhanced decoding, addressability and parallelizability"
title_zh: 用于非Clifford逻辑的量子群码：增强的解码、可寻址性和并行性
authors: "Jean Gasnier, Virgile Guémard"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27211v1"
tags: ["query:qec-decoding"]
score: 8.0
evidence: 量子群码与增强解码
tldr: "量子纠错码中实现非Clifford逻辑门需要可寻址并行的多控制Z门。本文提出量子群码框架，通过类域论提升经典AG码构造，支持可寻址和并行化的C^{m-1}Z门。解码复杂度从先前量子AG码的三次时间降至准二次时间，且保持线性解码半径。该工作使魔法态蒸馏协议的时间复杂度降低近线性因子。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27211v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 415, \"height\": 280, \"label\": \"Figure\"}]"
motivation: 实现高效的非Clifford逻辑门，且支持多控制Z门的可寻址和并行操作。
method: 基于类域论从经典AG码提升构造量子群码，获得可寻址并行的多控制Z门。
result: 解码复杂度降至准二次时间，解码半径为线性，比先前量子AG码降低近线性因子。
conclusion: 为魔法态蒸馏协议提供高效量子群码，显著提升并行性和解码效率。
---

## 摘要
我们引入了一个基于经典拟群码的框架，用于定义一类量子CSS码，称为量子群码，它支持可寻址且可并行的横向多控制-Z门，从而允许在逻辑层高效实现由非Clifford门组成的电路。在此基础上，我们利用从类域论建立的经典AG码的提升过程，构造了具有改进的解码复杂度和逻辑多控制-Z门并行性的良好量子群码。更准确地说，给定一个在字母表F_q上、具有横向C^m Z门的好量子AG码，我们将这一提升过程应用于其底层的经典AG码，得到一个在字母表F_{q^2}上的量子群码，该码支持横向C^m Z门以及可寻址且可并行的C^{m-1} Z门。此外，该量子码具有一个拟二次时间解码器，解码半径为线性。这与先前的量子AG码（具有三次时间解码器）形成了对比。因此，我们的工作意味着最先进的魔法态蒸馏协议的时间复杂度几乎减少了一个线性因子。

## Abstract
We introduce a framework based on classical quasi group codes to define a class of quantum CSS codes, called quantum group codes, supporting transversal multi-control-$Z$ gates which are both addressable and parallelizable, thus allowing to efficiently implement circuits composed of non-Clifford gates at the logical level. Building on this, we use a lifting procedure of classical AG codes established from class field theory to construct good quantum group codes with improved decoding complexity and logical multi-control-$Z$ gate parallelizability. More precisely, on input a good quantum AG code over the alphabet $\mathbb F_q$ with transversal $\mathsf{C}^m\mathsf Z$ gate, we apply this lifting procedure to its underlying classical AG code and obtain a quantum group code over the alphabet $\mathbb F_{q^2}$ supporting a transversal $\mathsf{C}^m\mathsf Z$ gate as well as addressable and parallelizable $\mathsf{C}^{m-1}\mathsf Z$ gates. In addition, this quantum code admits a quasi-quadratic time decoder with a linear decoding radius. This is to be compared with the previous quantum AG codes which have a cubic-time decoder. Hence, our work implies a decrease of the time complexity of state-of-the-art magic-state distillation protocols by an almost linear factor.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有量子容错计算方案（如魔法态蒸馏）需要实现非Clifford逻辑门（如多控制-\(Z\)门），但已有的量子码要么不支持可寻址（只能全局作用），要么解码复杂度过高（\(O(n^3)\)），限制了实际应用。
- **整体含义**：论文旨在设计一类新的量子CSS码——**量子群码**，使其同时具备以下特性：
  - 支持**可寻址且并行化**的横向非Clifford门（多控制-\(Z\)门）；
  - 解码复杂度从三次降低到**拟二次时间**，解码半径仍为线性；
  - 通过提升经典代数几何（AG）码，获得渐近良好的参数（\([[n, \Theta(n), \Theta(n)]]\) 或 \([[n, \Theta(n/\log n), \Theta(n)]]\)）。
- **背景**：目前非LDPC的量子AG码在魔法态蒸馏中已取得突破（如[WHY25]），但解码耗时。论文利用类域论构造的拟群码结构，同时提升了可寻址性和解码效率。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将经典**拟群码**（quasi-group code）的概念扩展到量子CSS码，利用群作用实现可寻址和并行横向门；再通过**类域论**的“提升”过程，将已有的好量子AG码转化为量子群码，保持良好参数并降低解码复杂度。
- **关键技术细节**：
  - **量子群码定义**：基于两个拟群码 \(C_0, C_1\) 构造CSS码，并要求商空间 \(C_1/C_0^\perp\) 是自由 \(\mathbb{F}_q[G]\)-模（\(G\)为有限群）。这保证了逻辑量子比特与群元素对应，从而可通过物理上作用 \(G\) 的位移来实现逻辑多控制-\(Z\)门的寻址。
  - **可寻址并行的充分条件**：定理3.4给出，若经典码\(C\)具有\(m\)-乘法性质且\(C^\perp\)距离大于轨道大小，则对其进行缩短/穿刺后得到的量子群码支持\(C^{m-1}Z\)门的深度一实现，且能并行作用在轨道内任意逻辑量子比特对上。
  - **提升构造**：从满足乘法性质的经典AG码\(C=\mathrm{C_L(P,D)}\)出发，通过未分歧阿贝尔扩张（类域论）提升到更大域\(\mathbb{F}_{q^2}\)，得到的新码\(\tilde{C}\)是自由\(\mathbb{F}_{q^2}[G]\)-模，群阶\(\log|G| = \Theta(n)\)（与原始码长线性）。提升过程保持乘法性质，从而量子群码继承可寻址门。
  - **高效解码**：利用群代数上的快速傅里叶变换，将AG解码算法从\(O(n^3)\)优化到\(O(n^2\cdot\mathrm{polylog}\,n)\)（定理4.7），且保持线性解码半径。

### 3. 实验设计：使用了哪些数据集/场景，benchmark，对比方法

- **数据集/场景**：论文是纯理论构造，没有使用传统实验数据集。主要“实验”是**复杂度分析和渐近参数推导**。
- **Benchmark**：对比对象是已有的量子AG码（如[WHY25]）和可寻址量子码（如[HVWZ25a, HVWZ25b, Gué25]）。比较指标为：
  - 解码时间复杂度：\(O(n^3)\) vs \(O(n^2\cdot\mathrm{polylog}\,n)\)
  - 可寻址程度：全局门 vs 轨道内全寻址
  - 码参数：渐近率、距离、维度与长关系
- **对比方法**：论文明确对比了：
  - 传统量子AG码解码器（三次时间）
  - 此前可寻址量子码（如基于Stichtenoth塔的构造）
  - 魔法态蒸馏协议的时间复杂度

### 4. 资源与算力：GPU型号、数量、训练时长等

- **未提及**：论文是数学与编码理论工作，不涉及实际量子或经典硬件实验，因此没有提供任何算力资源信息。

### 5. 实验数量与充分性：是否充分、客观、公平

- **实验数量**：无数值实验。理论证明部分覆盖了多种情形：
  - 两种参数选择：正则量子群码（单轨道，维度 \(\Theta(n/\log n)\)）和自由量子群码（多轨道，维度 \(\Theta(n)\)）
  - 适用于任意 \(t>4\)（2的幂），\(q=t^4\)，以及 \(1<m\le t/2-1\) 的多控制-\(Z\)门
  - 解码算法复杂度分析与线性半径证明
- **充分性评估**：
  - 所有结论均有严格数学证明（引理、定理、推论），涵盖参数界、门实现、解码算法。
  - 对比了已有工作的复杂度，给出了更优的渐近下界。
  - 没有进行模拟或交叉验证，但对于理论编码论文而言，证明已足够充分、客观。公平性体现在直接引用并比较了他人工作的复杂度。

### 6. 论文的主要结论与发现

- **结论1**：存在两类渐近良好的量子群码：
  1. **正则型**：\([[n, \Theta(n/\log n), \Theta(n)]]\)，支持完全可寻址的并行 \(C^{\widetilde{m}-1}Z\) 门（对任意 \(\widetilde{m}\le m\)），任意逻辑电路深度 \(O(k^{m-1})\)。
  2. **自由型**：\([[n, \Theta(n), \Theta(n)]]\)，仍保持全局横向 \(C^m Z\) 门，并支持轨道内可寻址的 \(C^{\widetilde{m}-1}Z\) 门，轨道数 \(\Theta(\log n)\)。
- **结论2**：两类量子码均具有 \(O(n^2\cdot\mathrm{polylog}\,n)\) 时间解码器，解码半径为线性。
- **推论**：使用该码的魔法态蒸馏协议（CCZ magic state）时间复杂度从 \(O(n^3)\) 降至 \(O(n^2\cdot\mathrm{polylog}\,n)\)，且开销常数。

### 7. 优点：方法或实验设计上的亮点

- **理论创新**：
  - 首次将“拟群码”与量子CSS码结合，通过自由群模结构自然实现可寻址并行横向非Clifford门。
  - 利用类域论提升经典AG码，同时保持乘法性质并增大自动群规模，一举两得。
- **计算效率提升**：将解码复杂度从三次降至拟二次，是显著的渐近改善。
- **灵活性**：通过调整轨道数量（\(r\)）可在“可寻址程度”与“维度”之间权衡，适应不同应用需求。
- **适用性强**：覆盖任意大于4的2的幂次 \(t\)，对当今量子计算常用的二进制或qudit系统均适用。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **未做数值验证**：论文没有提供任何蒙特卡洛模拟或小规模数值测试来验证实际解码性能或门误码率，所有结论基于渐近理论。实际有限长度下性能未知。
- **有限适用条件**：构造要求 \(q=t^4\) 且 \(t>4\) 为2的幂，限制了字母表大小的选择；且要求底层AG码满足特定的乘法性质和双轨良好性，并非所有AG码都适用。
- **群结构限制**：需要群 \(G\) 的阶要么是素数的幂要么与其互素（为保证解码算法可行），这通过子域调整可以实现，但会损失一半的群大小（由 \((\sqrt{q}-1)^{2g}\) 降为 \((\sqrt{q}-1)^g\)）。
- **物理实现**：论文专注于逻辑门和解码的理论性质，未讨论物理实现中的实际噪声模型、门保真度或错误传播等工程细节。

（完）
