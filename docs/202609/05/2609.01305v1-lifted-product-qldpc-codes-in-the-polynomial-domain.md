---
title: Lifted-Product QLDPC Codes in the Polynomial Domain
title_zh: 多项式域中的提升积QLDPC码
authors: "Vahid Nourozi, Mohsen Moradi, Roxana Smarandache, David G. M. Mitchell"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.01305v1"
tags: ["query:qec-code"]
score: 10.0
evidence: 直接研究量子低密度奇偶校验码构造，命中核心检索主题
tldr: 针对有限长提升积QLDPC码直接在二进制域构造时缺少代数分析工具的问题，本文提出多项式域表述。基矩阵定义在商环上，逐项提升为循环块，从而能在二进制展开前验证CSS正交性。该方法证明循环提升与多项式共轭兼容，确保二进制码满足CSS约束。用NASA卷积码和3×4矩阵示例实现构造，并在去极化信道下评估了有限长性能，为QLDPC码设计提供结构化框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.01305v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 918, \"height\": 590, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.01305v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 916, \"height\": 588, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.01305v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 911, \"height\": 613, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.01305v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 592, \"height\": 406, \"label\": \"Table\"}]"
motivation: 在二进制矩阵上直接构造提升积QLDPC码难以预判CSS约束，本文引入多项式商环以在展开前分析。
method: 将多项式基矩阵逐项提升为循环块，证明该提升与多项式共轭兼容，从而保证CSS正交条件。
result: 结合约束长度7的NASA卷积码与3×4矩阵示例构造出CSS兼容码，去极化信道解码评估显示框架有效。
conclusion: 该框架为从小的多项式基矩阵系统构造有限长QLDPC码提供了结构化途径。
---

## 摘要
本文提出了有限长度多项式域的提升积量子低密度奇偶校验（QLDPC）码的构造方法。我们在商环F 2[D]/(D L + 1)上构造码，其中多项式基矩阵被逐项提升为二元循环块。这种表示给出了提升积奇偶校验矩阵的紧凑代数描述，并允许在二元扩展之前分析CSS正交性。我们证明了标准循环提升映射与多项式共轭兼容，这意味着所得二元矩阵满足CSS对易约束。该构造以约束长度为7、码率为1/2的NASA卷积码为例进行说明，并提供了来自3×4多项式奇偶校验矩阵的数值示例。随后，使用各种基准解码器在去极化信道上评估所选构造码的有限长度性能。所得框架为从小型多项式基矩阵构造有限长度提升积QLDPC码提供了一种结构化方法。

## Abstract
This paper presents a finite-length polynomial-domain formulation of lifted-product quantum low-density parity-check (QLDPC) codes. We formulate the code construction over the quotient ring F 2[D]/(D L + 1), where polynomial base matrices are lifted entrywise to binary circulant blocks. This representation gives a compact algebraic description of the lifted-product parity-check matrices and allows CSS orthogonality to be analyzed before binary expansion. We show that the standard circulant lifting map is compatible with polynomial conjugation, which implies that the resulting binary matrices satisfy the CSS commutation constraint. The construction is illustrated with a constraint length 7, rate 1/2 NASA convolutional code example, and numerical examples are provided from a 3 x 4 polynomial parity-check matrix. The finite-length performance of selected constructed codes is then evaluated over the depolarizing channel using various benchmark decoders. The resulting framework gives a structured method to construct finite-length lifted-product QLDPC codes from small polynomial base matrices.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与研究动机

- 论文聚焦于**量子低密度奇偶校验（QLDPC）码**的构造与分析。
- 量子纠错需要的 CSS 码要求两个二元奇偶校验矩阵满足正交条件 \(H_X H_Z^T = 0\)。
- 在既有的**提升积（Lifted-Product, LP）构造**中（如 Panteleev–Kalachev 的工作），码常常是在二元迭代矩阵域直接构造。这类表示的缺点是：码长较大时，CSS 正交性只能通过生成完整的二元矩阵来验证；缺少紧凑的代数结构来引导有限长码的设计与搜索。
- 因此，本文的核心问题是：**能否找到一种有限长、多项式域的代数表述，使 LP-QLDPC 码能在“二元展开前”验证 CSS 正交性并紧凑描述奇偶校验矩阵**。
- 整体意义：将经典卷积码/准循环码的多项式方法引入现代提升积量子码设计，为有限长结构化 QLDPC 码提供一个可验证、可搜索的设计框架。

## 2. 论文提出的方法论：核心思想与技术细节

- **代数基础**：
  - 将多项式基矩阵定义在商环 \(R_L = \mathbb{F}_2[D]/(D^L+1)\) 上。由于特征为 2，这里 \(D^L=1\)，适合表示循环结构。
  - 定义多项式共轭 \(p^*(D)=p(D^{-1})\)，即把 \(D^i\) 映到 \(D^{-i} \pmod L\)。
  - 定义“逐项循环提升”（lifting）操作 \(L_L\)：把每个多项式 \(p(D)\) 映射为 \(L\times L\) 二元循环矩阵，系数为 1 的项对应循环移位矩阵 \(\mathrm{Circ}_L(i)\)。

- **关键性质证明**：
  - 证明了循环提升是环同态：\(L_L(pq)=L_L(p)L_L(q)\)、\(L_L(p+q)=L_L(p)+L_L(q)\)。
  - **核心贡献是“共轭兼容性”引理**：任意 \(p\in R_L\)，都有  
    \[
    L_L(p^*) = L_L(p)^T.
    \]
    即“先共轭再提升”等价于“先提升再转置”。
  - 由此可把矩阵的共轭转置操作由二元矩阵域上转换到多项式域中。

- **LP 多项式校验矩阵构造**：
  - 给定两个多项式基矩阵 \(B_1\in R_L^{m_1\times n_1}\)、\(B_2\in R_L^{m_2\times n_2}\)，构造块矩阵
    \[
    B_X = [B_1\otimes I_{n_2} \mid I_{m_1}\otimes B_2^*],
    \]
    \[
    B_Z = [I_{n_1}\otimes B_2 \mid B_1^*\otimes I_{m_2}].
    \]
  - 对它们逐项提升后得到二元矩阵 \(H_X=L_L(B_X)\)、\(H_Z=L_L(B_Z)\)。

- **正交性定理**：
  - 在多项式域证明 \(B_X B_Z^* = 0\)，证明使用了 Kronecker 积混合乘规则、共轭转置性质以及 \(\mathbb{F}_2\) 特征 2 下两相同项相加为零。
  - 再借助循环提升的同态性与共轭兼容性，推出二元域上的 CSS 条件 \(H_X H_Z^T = 0\)。
  - **意义**：CSS 正交性可以在较维数更小的多项式矩阵层验证，而不必先展开整个二元矩阵。

## 3. 实验设计

- 论文不使用传统“数据集”，而是以**人工构造/搜索得到的多项式基矩阵**作为输入；评估场景是**去极化信道（depolarizing channel）**。
- 实验分三类：
  1. **示例性构造**：用 NASA 约束长度 7、码率 1/2 卷积码的多项式矩阵 \(B(D)=[1+D+D^3+D^4+D^6,\ 1+D^3+D^4+D^5+D^6]\) 构建对称 \(B_1=B_2=B\)，通过 \(L=7\) 获得 \(N=35\) 的码，主要用于说明理论构造，没有做译码仿真。
  2. **参数表**：使用计算搜索得到的 \(3\times 4\) 多项式基矩阵 \(H(D)\)，令 \(B_1=B_2=H(D)\)，对多种提升尺寸 \(L=10,11,\dots,83\) 生成一系列有限长码。表中给出码长 \(N\)、维度 \(K\) 与最小距离上界 \(d_X,d_Z,d\)，小部分为精确值，大尺寸时仅为上界。
  3. **译码性能仿真**：对 \([[425,21,18]]\)、\([[600,32,d\le20]]\)、\([[2075,87,d\le34]]\) 三个码进行了有限长误块率（BLER）评估。其中最小距离搜索使用 Gurobi 优化器，但只给出上界。

- 对比的基准解码器：
  - QBP（四进制置信传播），设最大迭代 \(T=10000\)；
  - QBPGD（带引导去伪的置信传播），\(T=100\)，去伪轮数 \(G=N\)，总复杂度为 \(GT\)；
  - RL-S（基于强化学习的顺序 BP 解码），迭代预算 \(T=100\) 或 \(1000\)；
  - 对比指标为去极化信道下的误块率（BLER）。

## 4. 资源与算力

- 论文**没有明确报告任何硬件资源**（如 GPU 型号、数量、训练时长、能耗等）。
- 仅从文本可知 RL-S 策略需要“离线训练”，但训练过程的计算开销完全未知。
- QBPGD 设置了较重的迭代预算（例如 \(GT=207500\)），说明其译码仿真有较高计算成本，不过作者也未给出运行时间或复杂度对比。
- 由于缺乏资源信息，第三方复现或评估实验代价会比较困难。

## 5. 实验数量与充分性

- 表 I 覆盖了 11 组提升尺寸 \(L\) 的码参数，展示了码长从 250 到 2075 的码族。
- 真正进入译码仿真的只有三组码（L=17、24、83），并且没有实现不同基矩阵之间的系统性比较，也没有与其他主流 QLDPC 构造（例如 hypergraph-product、balanced-product 或随机 lifted-product）做端到端性能对比。
- 没有进行消融研究，例如验证不同选择 \(B_1,B_2\)（不同稀疏度、不同行重）对性能的影响；也没有系统扫描解码超参数。
- 因此实验可以**说明框架“可以工作”**：所得码确实满足 CSS 条件，也能被 BP 类解码器译码；在有限尺度上，RL-S 的改善一致性较强。但从编码理论全面评价的角度看，**证据相对有限，并非大规模基准测试**。

## 6. 主要结论与发现

- 在商环 \(R_L\) 上成功建立了有限长 LP-QLDPC 码的等价多项式表示，可**在二元展开前证明 CSS 正交性**。
- 发现循环提升与多项式共轭的“兼容性”是整个机制的关键：它把转置运算换为多项式共轭，使定理证明简洁且可扩展。
- 该框架把经典卷积码的循环/卷尾提升描述与 LP 量子码联系起来，为利用卷积码搜索技术构造有限长 QLDPC 码提供了一个新路径。
- 数值结果说明，所构造的码可以被 QBP、QBPGD 与 RL-S 等 BP 类解码器有效处理；
- 在包含大量 4-环的长码上，RL-S 相对 QBP/QBPGD 可消除明显的错误平层。
- 作者强调：这是为**新的 LP-QLDPC 码设计**提供的描述与验证框架，并非新的渐进理论结果。

## 7. 优点

- 理论表述紧凑漂亮，把 LP 构造的“CS 约束”压缩到多项式恒等式 \(B_XB_Z^*=0\)，大幅减少了验证复杂度。
- 证明步骤较常规但仍严谨：从单项式验证共轭兼容性，再经线性性扩展到矩阵，逻辑清晰。
- 同时连接经典信道编码文献（卷积码、多项式模 \(D^L+1\) 表示），便于借鉴已有基矩阵设计经验。
- 构造出的码族在参数上可控（不同 \(L\) 对应不同 \(N,K\)），并给出了有限长距离界，具有较好的实用约束。
- 引入 RL-S 等先进解码器做实验，展示了与迭代译码相结合的可能性，而不仅仅是理论存在性证明。

## 8. 不足与局限

- **距离结果不完备**：较大码的最小距离只有基于搜索的上界（如 \(d\le20\)、\(d\le34\)），在证明上没有确切距离值，难以充分判断码质量。
- **性能实验覆盖窄**：只对 3 个码做了译码仿真，没有与同码长下其他 QLDPC 构造在 BLER 曲线上的对比，也没有给出编码/译码复杂度比较，故“性能竞争力”未得到充分佐证。
- **基矩阵选择单一**：\(3\times4\) 矩阵是通过搜索得到的，但论文没有说明搜索空间、优化目标和选择标准，具有一定偶然性，可能引入选择偏差。
- **算力信息缺失**：没有提供训练 RL-S 策略的硬件和耗时，也没有报告各译码器平均时延，这对结果公平性评估不利。
- **个别结果的机制解释较粗略**：如大码上 QBP/QBPGD 出现错误平层被认为与大量 4-环有关，但作者没有用其他构造或消融实验验证这一因果关系。
- 该工作本质上是“另一种表示/验证框架”，并未声称产生新的渐进最优码；对追求高码率、距离的读者而言，实际编码增益仍需要进一步挖掘。

（完）
