---
title: Quantum Hierarchical Locally Recoverable Codes
title_zh: 量子分级局部可恢复码
authors: "Venkatesan Guruswami, Rutuja Kshirsagar, Pranav Trivedi"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06736v1"
tags: ["query:qec-decoding"]
score: 9.0
evidence: 提出量子层级局部可恢复码，包含高效解码算法
tldr: "量子局部可恢复码允许丢失量子比特通过少量其他比特恢复，但现有方案缺乏多层恢复能力。本文扩展(r,δ)量子局部可恢复码，给出基于量子Tamo-Barg的显式构造和高效解码。进一步提出量子分层局部可恢复码（QHLRC），实现多层局部恢复，并构造随机和显式h层码，建立Singleton-like界。这些成果为量子存储中高效擦除恢复提供了理论基础和实用架构。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06736v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 546, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06736v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 687, \"height\": 660, \"label\": \"Table\"}]"
motivation: 现有量子局部可恢复码仅支持单层恢复，灵活性不足；需要扩展到多层局部恢复以降低资源开销并提升恢复效率。
method: "基于经典Tamo-Barg构造和CSS框架，设计(r,δ)和分层量子局部可恢复码，给出随机与显式构造及高效解码算法。"
result: 提出了量子分层局部可恢复码，给出h层显式构造和高效解码，并建立了Singleton-like性能界限。
conclusion: 推进了量子擦除恢复理论，为高效量子存储系统设计提供了新方案和性能保证。
---

## 摘要
量子局部可恢复码（QLRCs）近年来作为实现高效量子存储并具备局部恢复能力的框架而受到关注。与经典对应码类似，QLRCs允许仅使用少量其他量子比特来重构丢失的量子比特，从而降低恢复过程中的资源和操作开销。本文通过考虑具有局部性参数$r$和局部距离$δ\geq 2$的$(r,δ)$ QLRCs，扩展了对QLRCs的研究。我们给出了随机和显式$(r,δ)$ QLRCs的构造，包括基于量子Tamo-Barg构造的显式族。我们还为这些量子Tamo-Barg码提出了一种高效译码算法。此外，我们引入了量子分级局部可恢复码（QHLRCs），将局部恢复扩展到多个分级层次。对于任意整数$h\geq 2$，我们构造了随机和显式的$h$级QHLRCs，后者为$h$级量子Tamo-Barg码，并通过基于含对偶经典码的CSS框架建立了这些码的类Singleton界。这些结果推进了量子擦除恢复的理论基础，并有助于设计高效的量子存储架构。

## Abstract
Quantum locally recoverable codes (QLRCs) have recently gained attention as a framework for achieving efficient quantum storage with local recovery capabilities. Analogous to their classical counterparts, QLRCs allow a lost qudit to be reconstructed using only a small subset of other qudits, thereby reducing the resource and operational overhead in recovery. In this work, we extend the study of QLRCs by considering $(r,δ)$ QLRCs characterized by locality parameter $r$ and local distance $δ\geq 2$. We present constructions of both random and explicit $(r,δ)$ QLRCs, including explicit families based on the quantum Tamo--Barg construction. We also present an efficient decoding algorithm for these quantum Tamo--Barg codes.   Furthermore, we introduce quantum \emph{hierarchical} locally recoverable codes (QHLRCs), which extend local recovery to multiple hierarchical levels. For any integer $h\geq 2$, we construct both random and explicit $h$-level QHLRCs, the latter being $h$-level quantum Tamo--Barg codes, and establish a Singleton-like bound for these codes using a CSS framework built from dual-containing classical codes. These results advance the theoretical foundations of quantum erasure recovery and contribute to the design of efficient quantum storage architectures.

---

## 论文详细总结（自动生成）

好的，根据您提供的论文内容，以下是对该论文的详细中文总结。

---

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的量子局部可恢复码（QLRC）主要关注单层局部恢复（即一个丢失的量子比特可由少量其他本地量子比特恢复），但缺乏在多层结构中进行灵活、高效恢复的能力。这在分布式量子存储等场景中限制了修复的灵活性和资源效率。
- **整体含义**：本文旨在将经典纠错码中成熟的“分级局部可恢复码（HLRC）”概念推广到量子领域，解决量子系统中因“不可克隆”约束而无法直接移植强经典局部性（如“可用性”）的问题。通过引入分级结构，允许在小型局部组处理常见擦除，而更大的组仅在必要时用于处理更严重的擦除模式，从而在降低资源开销的同时提升整体恢复效率。
- **研究背景**：先前工作聚焦于单层QLRC（如Golowich和Guruswami提出的经典QLRC和量子Tamo-Barg码）或纯最优的量子$(r,δ)-$LRC。本文的工作是互补性的，不追求最优全局距离，而是侧重于通过分级结构优化修复结构，并提供了广泛的构造和理论保证。

### 论文提出的方法论：核心思想与关键技术

论文的核心思想是构建基于CSS框架的量子分级局部可恢复码（QHLRC），其关键技术包括：

1.  **定义与框架**：
    - **量子局部分级局部可恢复码（QHLRC）**：通过递归方式定义。一个具有$((r_1, δ_1), ..., (r_h, δ_h))$参数的h级QHLRC，要求每个坐标都属于一个大小为$r_1+δ_1-1$的顶级局部组，该顶级组能纠正$δ_1-1$个擦除。并且，在该顶级组上穿刺（puncturing）后的子码本身又是一个具有$((r_2, δ_2), ..., (r_h, δ_h))$参数的$(h-1)$级QHLRC。这为分层恢复提供了严格的数学定义。
    - **CSS框架**：几乎所有构造都基于对偶包含的经典码。如果经典码$C$满足$C^\perp \subseteq C$，并且$C$是一个经典$(r,δ)-$LRC，那么CSS码$Q = CSS(C, C)$就是一个量子$(r,δ)-$QLRC。

2.  **关键构造步骤**：
    - **随机量子$(r,δ)-$QLRC和随机$h$级QHLRC**：通过精心设计随机校验矩阵来实现。校验矩阵由两部分组成：
        - **确定性局部分量**：使用局部Vandermonde矩阵块，这些块在互不相交的局部组上提供$δ-1$个本地校验。这些校验保证任何少于$δ$个错误的擦除模式都能被本地恢复。对于h级结构，这些本地组是嵌套的。
        - **随机全局分量**：在满足CSS正交性条件下，随机添加$ℓ$个全局行到X和Z校验矩阵中。随机性确保了码具有良好的全局距离。
    - **显式构造：量子Tamo-Barg (QTB) 码**：
        - **单级$(r,δ)-$QTB**：利用有限域$F_q$中$n=r+δ-1$次单位根的结构。通过精心设计指数集合$S$（包括用$S_+$和$S_-$表示的“好”和“坏”指数），使得经典码$C = ev(F_q[X]_S)$是一个$(r,δ)-$LRC，并且满足$C^\perp \subseteq C$。其核心是使用$S_+$来提供局域性校验。
        - **h级QTB码**：通过对每层使用嵌套的单位根子群，将单级QTB码直接推广到分级情况。每层的正指数集$S_{l,+}$都对应着该层的局部校验。
    - **折叠QTB码和折叠分级QTB码**：通过将连续的$s$个标量位置合并成一个更大的字母表符号（如$F_q^s$），在保持码率和局部性的同时，降低了全局禁字数。折叠操作利用单位根与折叠组间的交集性质（最多一个点），确保了局部恢复组的完整性。

3.  **关键证明技巧与公式**：
    - **非消失定理（Non-vanishing Theorem）**：这是距离下界证明的核心。该定理证明了对于特定多项式$Q_b$（在给定$δ-1$个单位根处为零），它在其他所有单位根上都不为零。这个结论在特性数有限域上也成立（需排除有限个特征）。该定理保证了变换后的多项式非零，从而能有效进行根计数。
    - **距离下界证明**：通过构造一个多项式$G$（乘积），其根的数量被下界约束，且其度数被上界约束，从而导出关于码字重量的不等式。
        - **单级距离下界**：利用凸函数$\psi(t) = (r-t)^+(n-(δ-1)t)^+$和Jensen不等式，将局部权重分配给本地组，然后全局求和，得到距离的闭式下界。
        - **分级距离下界**：递归应用上述单级技术，通过`Θ_h(t)`函数和递归定义的`Ψ_l(t)`函数，将分级距离下界表达出来。
        - **折叠码距离下界**：采用双模式分析：
            - **模不确定性原理**：当载波支撑$m_f$较小时，使用量子傅里叶变换不确定性原理（如Tao或Meshulam版本）来限制码字的全局权重。
            - **行列式多项式**：当非对偶部分支撑$m_g$较大时，通过构造行列式多项式$G(X)$，将其根的阶数（等于矩阵秩的亏缺）与折叠后权重联系起来，从而导出更强的下界。
    - **解码算法**：通过线性变换（由多项式`Q_i`定义）将QTB码的解码问题转化为Reed-Solomon码的列表解码问题。算法迭代所有可能的`i`，对变换后的码字进行列表解码，并验证候选解是否属于正确的陪集。

### 实验设计：数据集 / 场景及对比

- **数据集和Benchmark**：本文是理论构造为主，没有使用传统意义上的数据集或Benchmark。
- **场景**：所有分析均基于定义在有限域$F_q$上、通过单位根构造的解析码。没有对实际量子硬件或模拟噪声进行测试。
- **对比方法**：
    - 在**距离下界**方面，将随机构造、显式QTB构造、折叠QTB构造与已知的量子Singleton-like界进行了对比，证明了所提出的下界在某些参数范围是有效的。
    - 在**有限长实例**上，论文通过一个两级QTB码的实例（表2），将其与单级QTB码的维数和距离进行了数值对比，以说明分级结构的主要优势在于改善修复结构，而不是提升全局距离。
- **结论**：论文没有提供与现有特定量子码（如量子LDPC码或其它量子LRC）的系统性性能对比实验，对比主要停留在理论界限层面。

### 资源与算力

论文中**没有**提及任何关于GPU型号、数量或训练时长等算力资源的信息。整个研究是纯理论性的，没有涉及需要大规模计算资源的实验。

### 实验数量与充分性

- **实验数量**：论文的理论性远大于实验性。主要的“实验”包括：
    1.  多个存在性证明（随机构造的概率保证）。
    2.  一个有限长实例（表2），对比了2级与单级QTB码的性能。
    3.  距离下界的计算和比较（由理论公式直接给出）。
- **充分性**：从**理论证明**角度看，论文的构造和证明是系统且充分的，涵盖了随机、显式、单级、分级、折叠等多种情况，并提供了坚实的数学推导。然而，从**实验验证**角度看，其充分性不足。论文缺乏足够的仿真实验来验证理论下界的紧致性，也缺乏与其它竞争性量子码（如基于LDPC的QLRC）在实际擦除信道下的性能对比。仅有的一个有限长实例不足以全面评估分级结构的实际优劣。

### 论文的主要结论与发现

1.  **成功将分级局部恢复概念引入量子域**，定义了$h$级量子分级局部可恢复码（QHLRC），并建立了其理论框架。
2.  **提出了多种构造方法**：
    - 基于随机CSS矩阵的$(r,δ)-$QLRC和$h$级QHLRC，证明了其在高概率下能获得良好的距离-率-局部性权衡。
    - 基于Tamo-Barg码的显式QTB码，并推广至分级QTB码（HQTB）。这些码具有透明的局部结构和可分析的距离下界。
    - 通过折叠操作，提出了折叠QTB和折叠HQTB码，能在保持局部性和率的同时，获得更好的渐近距离下界。
3.  **证明了关键的局限性**：通过理论分析和实例（表2），论文明确指出了分级结构**不一定会自动改善全局最小距离**。分级的主要优势在于提供更灵活的修复结构，而非提升码本的全局强度。
4.  **提出了高效的解码算法**：为$(r,δ)-$QTB码设计了解码器，其纠错能力理论上界被严格推导（约等于距离下界的一半），且具有$(q^{O(δ)} \cdot \text{poly}(r, q))$的时间复杂度。

### 优点：方法或实验设计上的亮点

1.  **理论体系完整**：从定义、构造到性能（距离、局部性）和译码，形成了一个自洽、理论深度高的完整研究。
2.  **解决核心难题**：巧妙地克服了量子“不可克隆”限制给经典局部性概念扩展带来的困难，通过分级结构而非强局部性（如可用性）来提供修复灵活性。
3.  **证明技术新颖**：
    - **非消失定理**是连接代数结构和经典Reed-Solomon译码的关键桥梁，并成功避免了复合阶不确定性原理的弱点。
    - 二级证明技巧（不确定性原理 + 行列式多项式）为折叠码距离研究提供了范式。
4.  **构造方法全面**：同时提供了随机存在性证明和可显式构造的代数族，覆盖了从概率论到代数几何的不同视角。
5.  **坦率面对局限**：明确指出了距离下界不紧以及分级结构不提升全局距离的局限性，体现了学术研究的诚实性。

### 不足与局限

1.  **理论下界不紧**：如论文多次提到，推导出的距离下界（特别是分级情况）由于频繁使用Jensen不等式等因素，远非最优。实例（表2）显示，实际距离远大于理论下界。
2.  **缺乏大规模仿真验证**：作为一种理论论文，缺乏在实际量子信道下的性能仿真。没有提供与其它主流量子纠错码（如量子LDPC码、量子Skelton码）在擦除率、纠错能力、译码复杂度等方面的系统性对比。
3.  **分级结构效用未充分量化**：虽然论文论证了分级结构的合理性，但没有提供任何量化指标（如恢复延迟、资源消耗）来证明其在实用场景下的优越性。
4.  **参数限制较多**：
    - 显式构造依赖于有限域中存在特定阶的单位根（$n | q-1$）和排除有限坏特征数。
    - 随机构造假设了块长度的可分性和嵌套，这在长码构造中可能不够灵活。
    - 译码算法在$δ$固定时是多项式时间，但复杂度随$δ$指数增长（$q^{O(δ)}$），对于大的局部距离$δ$可能不实用。
5.  **实验覆盖有偏风险**：由于理论性过强，论文的“实验”部分（指计算对比）本质上是对自身构造优点的理论论证，而非在公平无偏场景下的性能评估。存在一定程度的证实性偏差风险。
6.  **应用限制**：理论假设（如CSS码对偶包含、均匀群大小）和应用场景（大规模量子存储）之间仍有较大差距。距离下界的不紧使得码的实际纠错能力未知。

（完）
