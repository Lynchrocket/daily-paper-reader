---
title: "Univariate Bicycle Quantum LDPC Codes: Explicit Logical Structure and Distance Bounds"
title_zh: 单变量自行车量子LDPC码：显式逻辑结构与距离界
authors: "Sheida Rabeti, Hessam Mahdavifar"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14173v1"
tags: ["query:qec-decoding"]
score: 9.0
evidence: 量子LDPC码构造与分析
tldr: 量子LDPC码设计面临复杂度挑战。本文提出单变量自行车(UB)码，通过Frobenius关系将广义自行车(GB)码的双多项式搜索简化为单多项式，保持稀疏性。显式构建逻辑算子基并导出最小距离上界。仿真显示，中等码长下UB码性能与GB码和双变量自行车码竞争。贡献在于以代数约束换取设计简化，且不牺牲性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14173v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 762, \"height\": 914, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14173v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 785, \"height\": 911, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.14173v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 869, \"height\": 502, \"label\": \"Table\"}]"
motivation: 简化广义自行车量子LDPC码的设计空间，同时保持稀疏性和性能。
method: 通过Frobenius约束将双多项式搜索简化为单多项式，构建逻辑商空间的显式代数基。
result: 推导了最小距离的上界，仿真显示UB码在中等码长下性能与GB/BB码竞争。
conclusion: UB码以额外代数约束简化设计，且不损失性能。
---

## 摘要
我们通过弗罗贝尼乌斯关系引入单变量自行车(UB)码，这是广义自行车(GB)量子低密度奇偶校验(LDPC)码的一个结构化子类。该构造将码设计空间从GB码中的二元多项式搜索缩减为单一多项式搜索，同时保持稀疏性。我们通过构造逻辑商空间的一组基，提供逻辑陪集空间的显式代数刻画，从而得到逻辑算符的完整参数化。利用这种结构，我们通过将结构化逻辑代表与相关循环矩阵的循环密度性质相关联，推导出最小距离的上界。最后，针对短到中等长度的UB码（块长从几百到约10^3）的仿真结果表明，尽管存在额外的代数限制，其性能相对于现有的GB码和双变量自行车(BB)码仍具有竞争力。

## Abstract
We introduce univariate bicycle (UB) codes, a structured subclass of generalized bicycle (GB) quantum low-density parity-check (LDPC) codes obtained via a Frobenius relation. This construction reduces the code design space from a two-polynomial search in GB codes to a single-polynomial search, while preserving sparsity. We provide an explicit algebraic characterization of the logical coset spaces by constructing a basis for the logical quotient space, yielding a complete parametrization of logical operators. Leveraging this structure, we derive upper bounds on the minimum distance by relating structured logical representatives to cycle-density properties of associated circulant matrices. Finally, simulation results for short- to medium-length UB codes (block lengths ranging from a few hundred to approximately $10^3$) demonstrate competitive performance relative to existing GB and bivariate bicycle (BB) codes despite the additional algebraic restriction.

---

## 论文详细总结（自动生成）

# 论文《Univariate Bicycle Quantum LDPC Codes: Explicit Logical Structure and Distance Bounds》详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：量子低密度奇偶校验（QLDPC）码是容错量子计算的关键。广义自行车（GB）码是一种灵活的构造，但需要搜索两个定义多项式，设计空间大、计算开销高。双变量自行车（BB）码虽性能优秀，仍需双多项式搜索。
- **核心问题**：如何在不牺牲码稀疏性和性能的前提下，显著简化GB码的设计过程，并提供显式的逻辑算符结构以支持后续分析（如距离界、逻辑门设计）。
- **整体含义**：本文提出**单变量自行车（UB）码**，通过Frobenius关系耦合两个多项式，将双多项式搜索降为单多项式搜索。更重要的是，该结构允许显式构造逻辑算符基，从而导出可计算的最小距离上界，并保持实际解码性能与GB/BB码相当。这为QLDPC码的系统化设计提供了新途径。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：在环 \( R_n = \mathbb{F}_2[x]/(x^n-1) \) 中，令 \( b(x) = a(x)^{2^\ell} \)（Frobenius映射），从而由单一多项式 \( a(x) \) 完全确定码的校验矩阵 \( H_X = [A, B], H_Z = [B^T, A^T] \)，其中 \( A = \text{Circ}(a(x)), B = \text{Circ}(b(x)) \)。该结构自动满足CSS正交性 \( H_X H_Z^T = 0 \)。
- **关键技术细节**：
  - **逻辑算符的显式基**（定理3）：当 \( a(x) \mid (x^n-1) \)（除数情形），构造了逻辑Z空间 \( L_Z \) 和逻辑X空间 \( L_X \) 的基。例如，\( L_Z \) 的基包含 \( Z^{(1)}_i = (x^i, (a^*)^{t-1}x^i) \) 和 \( Z^{(2)}_j = (0, h^* x^j) \)，其中 \( t=2^\ell, h=(x^n-1)/a \)，\( a^*, h^* \) 为互反多项式。这给出了逻辑类的完全参数化。
  - **最小距离上界**（定理5，推论4，推论6）：利用低权重逻辑代表（\( \alpha, \beta \) 多项式的重量之和 ≤ q）来限制距离。通过关联循环矩阵 \( C_r(f) \) 和 \( C_r(h) \)（其中 \( f = a^{t-1} \)）中诱导4-环和6-环的密度，得到 \( U_X^q, U_Z^q \) 的显式上界。例如重量1时，\( d \leq \min\{\text{wt}(a)^\ell+1, \text{wt}(h)\} \)。
- **算法流程**（文字说明）：① 选取整数 \( n \) 和多项式 \( a(x) \) 满足 \( a(x) \mid (x^n-1) \)；② 计算 \( t=2^\ell \)，\( b(x) = a(x)^t \)（模 \( x^n-1 \)）；③ 构造循环矩阵 \( A, B \)；④ 得到CSS码的校验矩阵；⑤ 利用定理3计算逻辑基，利用定理5计算距离上界；⑥ 搜索满足目标重量和距离的参数。

## 3. 实验设计
- **数据集/场景**：无传统数据集，而是基于特定多项式构造的码实例。仿真场景为**二进制对称信道**（BSC），仅考虑独立X型错误（对称性可推广至Z型）。
- **Benchmark**：与两类已发表的量子LDPC码比较：
  - **BB码**：Bravyi等（2024）的高阈值低开销BB码。
  - **GB码**：Panteleev & Kalachev（2021）的GB码实例（A1, A5, A6）。
- **对比方法**：不同码长、码率和稳定子重量的UB码与上述基准码在相同物理错误率 \( p_x \) 下比较逻辑错误率。解码采用BP-OSD-0（归一化最小和信念传播，因子α=0.875，最大迭代1000次）。

## 4. 资源与算力
- **未明确说明**：文中未提及任何具体的GPU型号、数量、训练时长或计算集群信息。仅提到仿真使用Python库（ldpc, codeDistancePYPI等）。实验规模较小（码长最多约1000），通常单机CPU即可完成，但作者未报告所用的硬件或运行时间。

## 5. 实验数量与充分性
- **实验数量**：主要呈现两个性能图（图1、图2）和一个参数表（表I）。图1比较了3对UB与BB码，图2比较了3对UB与GB码。此外，示例1和示例2展示了距离界与真实距离的对比。
- **充分性**：实验覆盖了短到中等码长（N≈40至1022），码率从0.032到0.176不等。每个物理错误率点收集至少150个逻辑错误，统计意义足够。但**缺乏消融实验**（如不同解码参数、不同信道模型、不同ℓ值对性能的系统影响），也未进行大规模长码（N>2000）的验证。仿真条件透明，对比公平（均使用BP-OSD-0），但基准码的数据来自文献，并非复现，可能存在实现差异。总体而言，实验数量适中，验证了方法可行性，但充分性有限。

## 6. 论文的主要结论与发现
- UB码通过Frobenius约束将设计空间从双多项式简化为单多项式，且**保持稀疏性**（\( \text{wt}(b) \leq \text{wt}(a) \)）。
- **显式逻辑基**的构造使得逻辑算符完全参数化，并允许导出**可计算的距离上界**（特别地，重量1和重量3上界在示例中与实际距离吻合）。
- 仿真结果表明，在短到中等码长下，UB码的性能**与BB码和GB码竞争，甚至更优**（例如\[234,26,14\] UB优于\[254,28,≤20\] GB码），且UB码通常具有略高的码率或更低的稳定子重量。
- 结论：以额外的代数结构换取设计简化，并不会显著损失性能，反而为理论分析和硬件实现提供了便利。

## 7. 优点
- **方法论创新**：首次提出利用Frobenius映射统一GB码的两个多项式，显著缩小搜索空间（从 \( O(n^w) \) 降至 \( O(n^{w/2}) \)）。
- **理论深度**：完整刻画了逻辑算符的代数结构（定理3），为距离界、逻辑门设计、编码理论分析提供了坚实基础。
- **距离界实用**：推导的诱导环密度界（定理5）计算简单，且低阶界（q=1,2,3）在实际中可达到或接近真实距离，优于通用的NP-hard精确计算。
- **实证效果**：在多个参数区间展示了与先进基准码相当甚至更好的纠错性能，说明了该构造的实用性。
- **表述清晰**：数学推导严谨，例证充分（如例1、例2）。

## 8. 不足与局限
- **理论覆盖不完整**：距离上界仅针对除数情形（\( a(x) \mid x^n-1 \)）推导，一般情形未处理；上界仅考虑q=1,2,3，更高的q未分析，可能漏掉更紧的界。
- **实验覆盖局限**：仅测试了中等码长（最大N≈1022），未涉及长码（如N>5000）或超大距离；未与其他新型QLDPC构造（如双曲型、扩展增长型）比较；缺乏对不同ℓ值的系统扫描，难以评估ℓ对性能的影响。
- **解码方法单一**：仅使用BP-OSD-0，未尝试其他解码器（如有序统计解码、置信传播增强、神经网络解码），可能限制了性能上限。
- **实际部署考虑不足**：未讨论电路级实现、门错误、可链接性等硬件相关方面；未分析UB码在有限大小效应下的阈值行为。
- **计算资源未报告**：缺少硬件和运行时间信息，不利于复现与公平评价。

（完）
