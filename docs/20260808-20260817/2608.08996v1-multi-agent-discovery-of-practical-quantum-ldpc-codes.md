---
title: Multi-agent discovery of practical quantum LDPC codes
title_zh: 多智能体发现实用量子LDPC码
authors: "Dongheng Qian, Tianyi Li"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.08996v1"
tags: ["query:qec-code"]
score: 10.0
evidence: 直接针对量子LDPC码的设计与发现
tldr: "量子LDPC码的有限长度实例设计需兼顾性能与实用约束，传统搜索困难。受AI智能体科学发现启发，提出多代理框架，结合专家提案与评审、持久科学记忆、长时程序进化及确定性构造评估，在闭环中搜索码。限制为n≤400、w≤10的二元CSS码，发现多个领先或竞争性码，如[[288,16,18]]等，并找到非正规子群作用的平衡积构造。解码测试显示低逻辑失效率，为实验提供候选。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.08996v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1573, \"height\": 905, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08996v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1725, \"height\": 1058, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08996v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1640, \"height\": 732, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.08996v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1420, \"height\": 938, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.08996v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1420, \"height\": 408, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.08996v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1275, \"height\": 599, \"label\": \"Table\"}]"
motivation: 有限长度qLDPC码搜索需在性能与实用约束间权衡，现有方法难以高效发现新码，需要引入智能体驱动的自动搜索。
method: 构建多代理框架，包含提案与评审代理、科学记忆库、程序进化和确定性评估，在coset-orbit平衡积码空间中搜索满足约束的码。
result: "在w=7,9,10分别发现最优码，如[[288,16,18]]、[[288,18,18]]、[[234,28,18]]，并得到非正规平衡积构造，BP-OSD下低逻辑失效率。"
conclusion: 工作展示结构化智能体搜索在量子码设计中的有效性，提供可硬件评估的候选码，推动实用qLDPC码发展。
---

## 摘要
量子低密度奇偶校验（qLDPC）码可以利用稀疏校验编码多个逻辑量子比特，但搜索有用的有限长实例仍然是一个具有挑战性的设计问题，因为码性能必须在满足实际约束的同时进行优化。受近期人工智能智能体在科学发现中应用的进展所启发，我们开发了一个用于发现实用qLDPC码的多智能体框架。该框架结合了专家提议与评审、持久科学记忆、可执行程序的长期演进，以及闭环搜索中的确定性构造与评估。这些程序实例化陪集轨道平衡乘积码，提供了一个包含自行车码和提升乘积码构造以及非正规子群作用的搜索空间。为了纳入实际约束，我们将搜索限制在块长$n\leq400$且总权重$w\leq10$的二元CSS码。在此范围内，该框架在所考虑的每个权重类别中发现了具有领先或具有竞争力的速率-距离性能的码，代表性实例包括$w=7$时的$[[288,16,18]]$码、$w=9$时的$[[288,18,18]]$码以及$w=10$时的$[[234,28,18]]$码。搜索还发现了结构上不同的高性能构造，包括一个$[[336,12,\leq24]]$候选码和一个$[[368,18,16]]$码，两者都是具有非正规子群作用的真正平衡乘积构造。在使用通用BP-OSD解码协议在码容量去极化噪声下评估时，所发现的码还表现出较低的逻辑失败率。这些结果共同为进一步的实验评估提供了与硬件相关的有限长候选码，并展示了结构化智能体搜索如何促进科学发现。

## Abstract
Quantum low-density parity-check (qLDPC) codes can encode multiple logical qubits using sparse parity checks, yet searching for useful finite-length instances remains a challenging design problem because code performance must be optimized while satisfying practical constraints. Motivated by recent advances in artificial-intelligence agents for scientific discovery, we develop a multi-agent framework for discovering practical qLDPC codes. The framework combines specialist proposal and review, persistent scientific memory, long-horizon evolution of executable programs, and deterministic construction and evaluation within a closed-loop search. These programs instantiate coset-orbit balanced-product codes, providing a search space that includes bicycle and lifted-product constructions as well as non-normal subgroup actions. To incorporate practical constraints, we restrict the search to binary CSS codes with block length $n\leq400$ and overall weight $w\leq10$. Within this regime, the framework discovers codes with leading or competitive rate--distance performance in every weight class considered, with representative instances including $[[288,16,18]]$ at $w=7$, $[[288,18,18]]$ at $w=9$, and $[[234,28,18]]$ at $w=10$. The search also uncovers structurally distinct, high-performing constructions, including a $[[336,12,\leq24]]$ candidate and a $[[368,18,16]]$ code, both of which are genuine balanced-product constructions with non-normal subgroup actions. When evaluated under code-capacity depolarizing noise using a common BP-OSD decoding protocol, the discovered codes also exhibit low logical failure rates. Together, these results provide hardware-relevant finite-length candidates for further experimental evaluation and show how structured agentic search can contribute to scientific discovery.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究背景**：量子低密度奇偶校验（qLDPC）码能够以稀疏校验编码多个逻辑量子比特，在渐近码率–距离权衡上优于表面码。然而，在有限块长、实际硬件约束（如码长、校验权重、参与校验的量子比特数）下，搜索高性能的 qLDPC 实例非常困难。传统方法通常依赖专家设计的构造族（如超图乘积、提升乘积、双变量自行车码）并在此框架内枚举或启发式优化，存在“构造空间窄则覆盖率低、构造空间广则优化难”的根本矛盾。
- **核心问题**：如何在有限长（\(n\le400\)）和总体权重（\(w\le10\)）约束下，高效发现兼具高码率、大距离、低解码错误率的实用 qLDPC 码，并同时探索结构多样化的新构造。
- **整体含义**：该论文提出一个多智能体（multi-agent）科学发现框架，将 AI 智能体用于 qLDPC 码的自动搜索，不仅找到了一批领先的有限长码实例，还展示了“结构化智能体搜索”可以作为科学发现的有效方法论，为硬件实现提供了候选码。

## 2. 方法论

- **核心思想**：构建一个闭环的“科学推理 + 程序进化”双循环框架。研究员委员会提出假设、审查方向；策展人将证据转化为可积累的经验（lesson/anti-lesson）；工人智能体（worker）通过长期进化的可执行程序生成候选码族；固定评估器进行确定性构造、验证与打分。系统将搜索对象从单个校验矩阵升级为“可生成整族码的程序”，实现多尺度结构变异。
- **搜索空间**：采用陪集轨道平衡乘积（coset-orbit balanced-product）码，该空间包含自行车码（bicycle）、提升乘积码（lifted-product）以及非正规子群作用下的真平衡乘积码。每个可执行码族分为四个层级：
  - L1：局域项（local terms）；
  - L2：原型图（protograph）形状；
  - L3：子群作用（subgroup action）；
  - L4：宿主群（host group）构造。
- **关键公式与约束**：
  - 总体权重定义为 \(w = \max\left\{\max_i \mathrm{wt}(H_{X,i}), \max_i \mathrm{wt}(H_{Z,i}), \max_q [\deg_X(q)+\deg_Z(q)]\right\}\)；
  - 编码率 \(k = n - \mathrm{rank}(H_X) - \mathrm{rank}(H_Z)\)；
  - 代理得分 \(Q_{\text{proxy}} = k \tilde d^2 / n\)，其中 \(\tilde d = \min(d_{\text{ub}}, 1.3\sqrt n)\)，\(d_{\text{ub}}\) 来自 QDistEvol 的距离上界；
  - 最终参数质量 \(Q = kd^2/n\)，其中距离 \(d\) 由 MILP 精确认证。
- **搜索策略**：
  - 使用 MAP-Elites 存档，以宿主群族、子群类型、码率区间、总体权重为niche坐标；
  - 选择父代时平衡代理分数、niche覆盖率和谱系新鲜度；
  - 距离估计采用分层预算：先 10⁴ 迭代，自动升级到 10⁵，再由策展人按需发放 10⁶ 迭代的高成本验证任务；
  - 固定评估器先检测 CSS 条件、正编码率、权重约束，再用 BLISS 规范标记去重。
- **解码性能评估**：采用代码容量去极化噪声模型，用最小和置信传播 + 序贯统计解码（BP–OSD）统一评估，报告块错误率 \(p_{\text{block}}\) 和每逻辑错误率 \(\epsilon_L = 1-(1-p_{\text{block}})^{1/k}\)。

## 3. 实验设计

- **数据集/场景**：没有传统数据集。搜索空间是 \(n \le 400\)、\(6 \le w \le 10\) 的二元 CSS 码。共搜索并分析了多个权重等级（w=6 至 10）。
- **Benchmark 对比**：
  - 文献基准集合：论文构建了一个包含 **91 个来源、1,209 个码实例** 的公开对比集，包含精确距离、上界、区间等不同距离证据类型；
  - 代表性基准码：双变量自行车码 [[288,12,18]]、[[360,12,≤24]]，权重十的广义自行车码 [[254,28,14–20]] 等；
  - 对比指标：在固定 \(w\) 下比较 \(Q = kd^2/n\)，并按距离证据类型（精确/上界）分开比较。
- **对比方法**：没有与“其他搜索算法”做端到端对比，而是与文献中已有的最佳码实例对比；在方法论上与 IBM 的 LLM 进化搜索 [26]、AlphaEvolve 式系统 [39,40] 进行了定性比较。
- **解码实验**：用统一 BP–OSD 协议，在 \(p=0.02,0.03,\dots,0.10\) 物理错误率下评估；每个点最多 10⁶ 次 shots，非自行车码在达到 10⁴ 个块错误后提前停止，自行车码统一 10⁶ 次 shots；计算伪阈值、95% 置信区间。

## 4. 资源与算力

- **论文未明确说明**具体使用的 GPU 型号、数量、训练或搜索时长等算力细节。
- 仅提及：所有智能体由 Claude Code 实现，研究员和策展人使用 Opus 4.8，工人使用 Sonnet 5；工人会话限制为 **40 个 agent turns**；策展人在每 25 个候选后调用，研究员议会每 50 个候选后调用（或在连续 8 个失败后触发）。距离验证使用 QDistEvol（进化算法）和 Gurobi 12.0.3（MILP），但未给出具体计算时间或硬件配置。

## 5. 实验数量与充分性

- **搜索规模**：覆盖 5 个权重等级（w=6–10），每个等级都有多个候选码；主表中列出 20 个突出发现码，另有 7 个结构独特码（Table II）。
- **验证实验**：
  - 参数评估：对多个码进行了精确距离 MILP 认证（如 [[288,16,18]]、[[288,18,18]]、[[234,28,18]]、[[378,32,19]] 等），对高距离候选进行了 10⁶ 次 QDistEvol 上界计算；
  - 解码模拟：Table III 列出了每个权重类的代表（每个权重类一个精确距离码和一个上界码），共 10 个码的伪阈值和不同物理错误率下的 \(\epsilon_L\)；图 3 展示了 w=6/7 的 4 个发现码与 2 个基准码的对比。
- **充分性评价**：
  - **优点**：实验覆盖了多个权重类，结果来自不同构造，且解码实验使用统一协议，未针对码调参，公平性较好；
  - **不足**：论文未披露所有找到的码的完整解码性能，仅展示部分代表；解码模拟仅在去极化噪声模型下进行，未考虑电路级噪声、测量错误、实际硬件限制；对照实验（如换掉某个智能体组件、改变调度策略）也未报道，缺乏消融实验来评估框架各模块的独立贡献。

## 6. 主要结论与发现

- 在每个权重类（w=6 至 10）中，框架都发现了具有领先或竞争力的速率–距离性能的码：
  - w=7：精确码 [[288,16,18]]，\(Q=18.00\)，为当前已知最强；
  - w=9：精确码 [[288,18,18]]，\(Q=20.25\)，超过先前已知最佳；
  - w=10：精确码 [[234,28,18]]，\(Q=38.77\)，以及 [[372,44,18]]，\(Q=38.32\)，超越此前最优；
  - w=8：精确码 [[378,32,19]]，\(Q=30.56\)，具竞争力；
  - 上界候选如 [[390,32,≤32]] \(Q\le84.02\)、[[384,16,≤24]] \(Q\le24.00\)，提示更佳码存在但尚未认证。
- 发现结构上多样化的构造：包括非正规子群作用的真正平衡乘积码（如 [[368,18,16]] 基于 \(A_6\times Z_2\)，[[336,12,≤24]] 基于 PSL(2,11)，[[248,12,18]] 基于 PSL(2,13) 等），以及经由非显然宿主群描述的提升乘积码（如 [[400,16,≤22]] 的中积构型、[[384,18,≤28]] 的 \(A_4\rtimes Z_{16}\)）。
- 解码性能：发现码在 BP–OSD 下与双变量自行车基准相当，其中 [[400,16,≤22]] 在所有采样错误率下优于两个基准；
- 伪阈值方面：所有测试码的伪阈值高于 5%（5.40%–9.30%），在 \(p=0.03\) 时所有码的 \(\epsilon_L\le1.68\times10^{-5}\)，大部分低于 \(10^{-6}\)。
- 观察到权重越高参数的 \(Q\) 越高，但 BP–OSD 伪阈值反而下降，说明当前解码器更匹配低权重的 Tanner 图。

## 7. 优点

- **方法论创新**：将多智能体科学推理与程序进化结合，形成“科学假设—证据积累—任务生成—程序变异—确定性评估”的闭环，超越了单纯依赖 fitness 反馈的 AlphaEvolve 式方法。
- **搜索空间广且有结构**：四层表示统一了局域项、原型图、子群作用、宿主群，可搜索自行车码、提升乘积码和真正的平衡乘积码，既宽泛又可解释。
- **距离验证分层分配**：将计算昂贵的距离估计按证据价值动态分配，提高验证预算效率。
- **实用性导向**：严格限制长度 \(n\le400\) 和总体权重 \(w\le10\)，并定义了包含校验权重和量子比特扇出的总体权重，更贴近硬件约束。
- **结果质量高**：多个码在精确距离下取得同类最优或接近最优的 \(Q\) 值，并提供完整的构造数据（补充信息），便于复现与实验。
- **客观评估**：去重使用规范标记，距离认证使用 MILP 全局最优，解码使用统一 BP–OSD 协议，未针对特定码调参。

## 8. 不足与局限

- **算力信息缺失**：未报告 GPU/CPU 数量、搜索墙钟时间、总评估次数等资源消耗，不利于复现成本评估。
- **缺少消融实验**：没有系统验证“研究员议会”“策展人记忆”“10⁶ 验证层”“MAP-Elites 调度”等各组件对最终性能的独立贡献。
- **仅覆盖有限参数空间**：只考虑 \(n\le400\)、\(w\le10\) 和二元 CSS 码，结论不一定外推到更长码长或其他码类。
- **距离证据不完全**：许多高 \(Q\) 候选（如 [[390,32,≤32]]）只有 QDistEvol 上界，未通过 MILP 认证；MILP 认证的计算成本导致无法对全部候选精确验证。
- **解码评估有限**：仅采用代码容量去极化噪声和单一 BP–OSD 实现，未评估电路级噪声、不同解码器、测量错误或实际硬件路由；对高权重码，BP–OSD 伪阈值较低，解码器与码的匹配需要进一步研究。
- **“真正平衡乘积”优势未体现**：论文自己也指出，非正规平衡乘积码在有限长参数上并未超过提升乘积码，其潜在优势可能只在更长码长或更宽松权重约束下出现，因此当前实用性尚不明确。

（完）
