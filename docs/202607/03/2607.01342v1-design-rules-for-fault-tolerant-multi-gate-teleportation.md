---
title: Design rules for fault-tolerant multi-gate teleportation
title_zh: 容错多门隐形传态的设计规则
authors: Mathys Rennela
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01342v1"
tags: ["query:qec-decoding"]
score: 8.0
evidence: 使用PyMatching进行多门隐形传态的表面码解码
tldr: 多门隐形传态通过单个纠缠对封装多个远程门，但网络故障会传播为高权重错误。针对旋转表面码，推导了故障容忍的包大小上限：使用相关感知解码器时为⌈d/2⌉，否则为⌊d/2⌋。模拟表明标准MWPM解码器自然纠正相关错误，在网络噪声主导时包化性能更优，且纠缠成本从n ebit降至1。该规则可指导分布式编译器在保证容错下采用扇出包化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01342v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 833, \"height\": 470, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01342v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 934, \"height\": 540, \"label\": \"Table\"}]"
motivation: 多门隐形传态虽能大幅节省纠缠资源，但网络故障引起的相关错误威胁容错性，需确定安全包大小。
method: 针对旋转表面码，理论推导了故障容忍包大小上限，并用PyMatching模拟评估了MWPM解码器在电路级噪声模型下的错误率。
result: 相关感知解码器下包大小可达⌈d/2⌉；标准MWPM解码器可自然纠正相关错误，在γ≤100时包化性能优于或等于顺序方案，且优势随γ和d增大。
conclusion: 扇出包化在网络瓶颈时显著降低纠缠成本且不牺牲容错性，无需定制解码器，可为分布式量子编译提供设计依据。
---

## 摘要
多门隐形传态（MGT）通过一个1-ebit扇出量子电路将n个远程门打包成一个ebit，相对于顺序门隐形传态节省了n-1个纠缠对。代价是相关的故障模式：单个网络故障通过扇出树传播，注入一个权重为n的泡利错误。我们推导了容错数据包大小的设计规则：对于距离为d的旋转表面码，使用相关性感知解码器时n_max^{corr}(d) = ⌈d/2⌉（没有时n_max^{naive} = ⌊d/2⌋），限制了在保持容错的同时可以打包多少个门。使用PyMatching的模拟表明，基于数据包电路噪声模型构建的标准MWPM解码器自然纠正了相关错误：在网络到本地噪声比γ = p_net/p_gate高达100时，数据包在中高γ下匹配或超过每链路顺序LER，优势随γ和d增长，同时将纠缠成本从n个ebit减少到1个。当网络是瓶颈（γ>>1）时，打包获胜；在γ≈1时，n-1个额外的本地扇出门抵消了网络节省。不需要自定义解码器：电路级噪声模型已经编码了相关性。这些结果使噪声感知的分布式电路编译器能够支持扇出打包而不牺牲容错。

## Abstract
Multi-gate teleportation (MGT) packages $n$ remote gates into a single ebit via a 1-ebit fan-out quantum circuit, saving $n{-}1$ entangled pairs relative to sequential gate teleportation. The cost is a correlated failure mode: a single network fault propagates through the fan-out tree, injecting a weight-$n$ Pauli error. We derive a design rule for fault-tolerant packet sizes, $\nmax^{\text{corr}}(d) = \lceil d/2 \rceil$ for rotated surface codes of distance~$d$ with a correlation-aware decoder ($\nmax^{\text{naive}} = \lfloor d/2 \rfloor$ without), bounding how many gates can be packaged whilst preserving fault tolerance. Simulation with PyMatching shows that the standard MWPM decoder built from the packet circuit's noise model naturally corrects the correlated error: at network-to-local noise ratios $γ= \pnet/\pgate$ up to $100$, the packet matches or surpasses the per-link sequential LER at moderate-to-high $γ$, with the advantage growing with both $γ$ and $d$, whilst reducing the entanglement cost from $n$ ebits to~$1$. Packetisation wins when the network is the bottleneck ($γ\gg 1$); at $γ\approx 1$ the $n{-}1$ extra local fan-out gates offset the network savings. No custom decoder is required: the circuit-level noise model already encodes the correlation. These results enable noise-aware distributed circuit compilers to favour fan-out packetisation without sacrificing fault tolerance.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在分布式量子计算（DQC）中，模块间通过远程门（如CNOT）通信，通常每个远程门消耗一个纠缠对（ebit）。多门隐形传态（MGT）通过单个ebit和本地扇出电路将`n`个远程门打包，节省`n-1`个ebit，但引入了一个严重问题：单个网络故障会通过扇出树传播，产生一个权重为`n`的相关Pauli错误。现有工作（如Shalby等）仅针对单门隐形传态，未建模这种相关错误。因此，需要研究MGT在量子纠错下是否仍能容错，以及最大安全包大小是多少。
- **核心含义**：确定在有噪声的分布式量子网络中，如何在保证容错的前提下最大化包大小，从而节省纠缠资源，为分布式编译器提供设计规则。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用表面码的纠错能力，结合最小权匹配（MWPM）解码器，分析相关错误对故障容限的影响，推导出最大安全包大小的解析界。
- **关键技术细节**：
  - **错误模型**：对相同控制的MGT包，一个网络故障（如X或Y错误）在控制量子比特上通过非局域CNOT传播到第一个目标，然后通过本地扇出CNOT复制到所有`n`个目标，形成一个权重为`n`的连续X错误串。Z错误不会产生权重-`n`串。
  - **设计规则**：
    - 对于距离为`d`的旋转表面码，一个相关感知解码器（将相关错误视为单条边）可纠正包大小`n ≤ ⌈d/2⌉`；而朴素解码器（视为`n`条独立边）只能纠正`n ≤ ⌊d/2⌋`。原因是：当`n = ⌈d/2⌉`时，互补错误权重为`⌊d/2⌋`，相关感知解码器中单条边的成本（`-ln p_net`）低于`⌊d/2⌋`条独立边的成本（`⌊d/2⌋·(-ln p_gate)`），从而选择正确纠正。
    - 当包大小超过`⌈d/2⌉`时，存在退化综合征且无低权重补集，两个解码器均失败。
  - **k-ary扇出树扩展**：若包大小超出最大值，可采用k元扇出树，使用`k`个桥接量子比特，每个扇出到约`n/k`个目标，将最坏错误权重降至约`n/k`，`k_min = ⌈n/⌈d/2⌉⌉`在1-ebit扇出（k=1）和全顺序（k=n）之间插值。

## 3. 实验设计

- **使用的场景/数据集**：采用简化电路级退极化噪声模型（仅门噪声），使用Eisert-Jacobs-Papadopoulos-Plenio (EJPP)协议实现非局域CNOT。基准比较对象为每链路顺序方案（每个远程门独立使用一个ebit，每个链路噪声率为`p_net`）。
- **对比方法**：
  - MGT包使用两种解码器：基于基础DEM（不含相关错误边）的朴素MWPM解码器（`LER_base`），以及基于包电路DEM（含`CORRELATED_ERROR`边）的相关感知MWPM解码器（`LER_pkt`）。
  - 顺序方案：每个远程链路独立出错，使用标准MWPM解码器（`LER_seq`）。
- **噪声参数**：`p_gate = 10⁻³`固定，`γ = p_net/p_gate`取值1, 10, 30, 100。代码距离`d = 5, 7, 9`。包大小`n`取相应距离下的最大允许值（如`d=5`时`n=3`）。

## 4. 资源与算力

论文未明确说明使用的GPU型号、数量或训练时长。仿真使用**PyMatching**进行DEM级MWPM解码，样本量为大多数点`10⁸`次射击（`d=5, d=7 at γ≤10, 所有d=9`），其余点`10⁶`次射击。未提及具体的硬件资源（CPU/GPU），仅说明计算在单个节点上完成，使用固定随机种子保证可重复性。

## 5. 实验数量与充分性

- **实验组数**：共9组主要实验（`d=5,7,9`各三个γ值：1,10,30,100，但论文表格列出`d=5`有γ=2,10,30,100；`d=7`有γ=2,10,30,100；`d=9`有γ=2,10,30,100，实际上γ=2仅出现在`d=5,7,9`的表第一行？仔细看：表I中`d=5`有γ=2,10,30,100；`d=7`有γ=2,10,30,100；`d=9`有γ=2,10,30,100。此外还有额外的d=5, n=3的验证。因此共12个（4个γ值×3个d）核心对比组。
- **充分性**：
  - 覆盖了不同的代码距离和噪声比，但未系统扫描所有可能的`n`值（仅测试了`n = ⌈d/2⌉`这一边界情况）。论文声称该边界是最坏情况（所有目标位于最小权重逻辑算子上），但未验证其他布局（如目标不在一条线上）。此外，仅考虑了X错误主导的相同控制配置，未专门测试相同目标配置（但指出行为对称）。
  - 噪声模型仅采用简化退极化（门噪声），未包括测量噪声、空闲噪声等，作者承认会低估绝对LER 3-5倍，但相对比较不受影响。
  - 缺乏对错误注入位置（如最终轮注入）的测试，这可能是更难解码的场景。
- **公平性**：比较是合理的：物理上对比每链路独立噪声（每个ebit独立使用），包化使用单一ebit。但顺序方案中每个远程链路使用一个ebit，而包化方案仅有1个ebit，但增加了`n-1`个本地扇出门，因此在低γ时略差（由于额外本地门噪声），高γ时明显更优。论文也承认了这一点。

## 6. 论文的主要结论与发现

- 对于旋转表面码，MGT包的安全大小上限为：使用相关感知解码器时`n_max = ⌈d/2⌉`，否则`n_max = ⌊d/2⌋`。
- 标准MWPM解码器（基于包电路的DEM自然包含相关错误边）**无需自定义修改**即可有效纠正相关错误，将容错包大小扩展一位。
- 在中等至高网络噪声比（`γ ≥ 10`）下，包化方案的逻辑错误率（LER）等同于甚至低于顺序方案，且优势随`γ`和`d`增大；在`γ ≈ 1`时包化略差（≤13%的惩罚），源自额外本地扇出门噪声。
- 包化显著节省纠缠资源：从`n`个ebit降至1个ebit，且不牺牲容错。
- 结论独立于隐形传态接口（EJPP、测量基、CAT态），因为相关错误源于单ebit扇出瓶颈。

## 7. 优点

- **清晰的设计规则**：提供了简洁的解析公式，可直接指导分布式编译器选择安全包大小，避免了数值扫描。
- **相关性感知的零成本**：证明标准MWPM解码器从自然DEM构建即可处理相关错误，无需定制解码器，降低了实现复杂度。
- **实际相关噪声比**：论文指出当前实验（如光子CNOT门保真度~87%，错误率~10⁻¹；纠缠生成错误率~5%）对应的`γ ∈ [50,100]`，正是包化优势最显著的区间，因此具有实际指导意义。
- **资源权衡分析**：详细比较了不同γ下的性能权衡，并提供了k-ary扇出树扩展方案以应对超大数据包。

## 8. 不足与局限

- **实验覆盖不充分**：
  - 仅测试了边界情况（`n = ⌈d/2⌉`），未系统扫描不同`n`值对LER的影响（如`n=2`或`n<d/2`时的表现）。
  - 错误注入位置固定为第0轮（最优情况），未测试最坏情况（如最终轮注入）。
  - 仅考虑X错误主导的相同控制配置，相同目标配置仅作对称性说明，未单独实验验证。
  - 噪声模型过于简化：仅门噪声，忽略测量噪声、空闲噪声、泄漏等。作者承认绝对LER可能低估3-5倍，但相对比较可信。
- **扩展性限制**：
  - 仿真仅限于旋转表面码和MWPM解码器，定性结论虽声称适用于任意CSS码，但缺乏数值证据。
  - 未扩展到量子LDPC码或其他非表面码架构。
  - 未考虑偏置噪声或非均匀噪声分布。
- **应用限制**：
  - 当`γ ≈ 1`时包化有轻微惩罚，编译器需考虑噪声比是否达到包化阈值。
  - 论文假设所有`n`个目标位于同一模块，且控制量子比特与目标位于不同模块。实际分布式系统可能有更多模块间连接，复杂拓扑下的推广未讨论。
- **资源开销**：未对k-ary扇出树的资源开销进行详细仿真（如k=2时的性能），仅给出理论k最小值公式。

（完）
