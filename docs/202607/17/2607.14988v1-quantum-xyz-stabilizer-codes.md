---
title: Quantum XYZ Stabilizer Codes
title_zh: 量子XYZ稳定子码
authors: "Alessio Baldelli, Davide Orsucci, Francisco Lázaro, Massimo Battaglioni"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14988v1"
tags: ["query:qec-code"]
score: 9.0
evidence: 提出非CSS结构的量子XYZ稳定子码
tldr: 传统CSS框架利用两个正交二元码构建X、Z稳定子，缺少Y型约束可能限制低权重逻辑算子的抑制和译码性能。本文提出量子XYZ稳定子码，采用三个两两正交的二元奇偶校验矩阵分别构建X、Y、Z型稳定子，并给出判断Y型校验是否冗余的代数条件。该方法导出了量子最小距离的上下界，并基于相交子集和拟对偶码族构造了稀疏有限长qLDPC实例。在退极化噪声和四进制BP译码仿真下，XYZ qLDPC码性能优于同类CSS码。贡献在于为非CSS量子码提供了新建构和理论分析。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14988v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 600, \"height\": 737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14988v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1374, \"height\": 624, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14988v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 796, \"height\": 500, \"label\": \"Table\"}]"
motivation: 传统CSS框架仅用X、Z型稳定子，缺乏非CSS约束，导致低权重逻辑算子抑制不足和有限长译码性能受限。
method: 基于三个两两正交的二元奇偶校验矩阵构建XYZ码，导出Y型稳定子冗余的代数与秩条件，并推导最小距离界。
result: 构造了XYZ²六边形码及稀疏有限长qLDPC码，仿真显示其性能优于类似参数的CSS qLDPC实例。
conclusion: XYZ稳定子码提供更丰富的非CSS结构，有望提升量子纠错码的有限长性能。
---

## 摘要
稳定子码通常在Calderbank-Shor-Steane（CSS）框架内构造，其中两个相互正交的二进制经典码定义X型和Z型稳定子生成元。虽然这种结构在代数上很方便，但额外的非CSS约束可能有助于抑制低权重逻辑算子并改善有限长度下的译码性能。因此，我们引入了量子XYZ稳定子码，其校验矩阵（PCM）由与X型、Y型和Z型稳定子生成元相关联的三个两两正交的二进制PCM构建。一个关键点是，XYZ码实例并不自动是真正的非CSS：同一个稳定子群可能允许一个CSS生成集。我们刻画了这种坍缩，获得了判定Y型校验何时冗余以及何时定义真正的非CSS稳定子约束的代数条件和秩条件。我们还推导了量子最小距离的上下界，包括混合泡利逻辑算子的界。这一新框架包含一个已知的非CSS拓扑码，即XYZ²六边形码，并且通过交集子集和拟对偶码族生成了稀疏的有限长度量子低密度奇偶校验（qLDPC）构造。在退极化码容量噪声和四元置信传播译码下的仿真表明，所提出的XYZ qLDPC实例可以胜过具有相似有限长度参数的代表性CSS qLDPC实例。

## Abstract
Stabilizer codes are often constructed within the Calderbank--Shor--Steane (CSS) framework, where two mutually orthogonal binary classical codes define $X$ and $Z$-type stabilizer generators. While this structure is algebraically convenient, additional non-CSS constraints may help suppress low-weight logical operators and improve decoding performance in the finite-length regime. We thus introduce quantum XYZ stabilizer codes, whose parity-check matrix (PCM) is built from three pairwise orthogonal binary PCMs associated with $X$-, $Y$-, and $Z$-type stabilizer generators. A nontrivial point is that an XYZ code instance is not automatically genuinely non-CSS: the same stabilizer group may admit a CSS generating set. We characterize this collapse, obtaining algebraic and rank conditions for deciding when the $Y$-type checks are redundant and when they define genuinely non-CSS stabilizer constraints. We also derive upper and lower bounds on the quantum minimum distance, including bounds for mixed Pauli logical operators. The novel framework includes a known non-CSS topological code, namely the XYZ$^2$ hexagonal code, and yields also sparse finite-length quantum low-density parity-check (qLDPC) constructions from intersecting-subset and quasi-dyadic code families. Simulations under depolarizing code-capacity noise and quaternary belief propagation decoding show that the proposed XYZ qLDPC instances can outperform representative CSS qLDPC instances with similar finite-length parameters.

---

## 论文详细总结（自动生成）

好的，根据您提供的论文内容，请参考以下对《Quantum XYZ Stabilizer Codes》的结构化总结。

### 论文核心问题与整体含义

*   **研究动机：** 量子稳定子码大多数基于Calderbank-Shor-Steane框架构建。CSS码虽结构便利，但仅使用X型和Z型两类稳定子。传统观点认为，引入非CSS约束（如Y型稳定子）有可能抑制低权重的逻辑错误，从而在有限码长下获得更好的译码性能。然而，对非CSS码的理论分析框架和构造方法相对缺乏。
*   **核心问题：** 如何系统地推广CSS框架以包含Y型稳定子，并分析新框架下码的代数结构、最小距离特性及实际性能优势？
*   **整体意义：** 本文提出了量子XYZ稳定子码，为非CSS量子码提供了一个通用的、可解析的构造框架。该框架不仅统一了已有非CSS拓扑码，还指导了新型有限长低密度奇偶校验码的构造，并在仿真中展现出优于同类CSS码的潜力，证明了放宽CSS限制在有限长性能提升上的价值。

### 论文提出的方法论

1.  **核心思想：** 将经典CSS码中由两个相互正交的二元经典码构造X、Z型稳定子的思想，推广到三个经典码（CX, CY, CZ）。这三个码对应的奇偶校验矩阵（HX, HY, HZ）需要满足两两正交的条件，以此保证所有稳定子算符的交换性。

2.  **关键技术细节：**
    *   **XYZ稳定子码的构造：** 码的校验矩阵定义为：
        \[
        H_{XYZ} = \begin{bmatrix} H_X & 0 \\ 0 & H_Z \\ H_Y & H_Y \end{bmatrix}
        \]
        其中，\(H_\sigma \in \mathbb{F}_2^{m_\sigma \times n}\) 是各类型稳定子的PCM。
    *   **“真正”XYZ码的判定：** 并非所有具有XYZ三种校验的码都是真正的非CSS码。论文给出了判断Y型校验是否冗余的**代数条件**：
        \[
        C_Y^\perp = (C_X^\perp \cap C_Y^\perp) + (C_Y^\perp \cap C_Z^\perp)
        \]
        以及相应的**秩条件**：
        \[
        rk(H_{XYZ}) = rk\left(\begin{bmatrix}H_X \\ H_Y\end{bmatrix}\right) + rk\left(\begin{bmatrix}H_Y \\ H_Z\end{bmatrix}\right)
        \]
        满足这些条件时，XYZ码坍缩为一个CSS码。
    *   **最小距离的界：**
        *   **上界**：通过分析纯X、Y、Z型逻辑算子的最小距离得到。例如，X型逻辑算子的最小距离 \(d_X = \min\{\|v\| : v \in (C_Y \cap C_Z) \setminus (C_X^{\perp} + C_X^K)\}\)，则上界 \(U = \min(d_X, d_Y, d_Z)\)。
        *   **下界**：通过构造其子码（如移除Y型校验得到的CSS码）的距离得到下界 \(L = \max(\tilde{d}_{X,Y}, \tilde{d}_{Y,Z}, \tilde{d}_{X,Z})\)。同时，还针对混合型逻辑算子推导了另一更紧的下界 \(B\)，最终下界为 \(\min(U, B)\)。

3.  **算法流程（文字说明）：** 设计一个XYZ码的流程是：首先选择三个两两正交的二元经典线性码，然后将它们的PCM按前述矩阵形式组合成XYZ码的PCM。码的代数结构和性能评估（如是否真正非CSS、最小距离的界）可以通过这个框架内的代数工具进行分析。

### 实验设计

*   **噪声模型与场景：** 采用**码容量退极化信道**作为基准噪声模型。
*   **Benchmark：** 选取了多种具有代表性的CSS qLDPC码实例进行对比，包括：
    *   拟对偶码
    *   相交子集码
    *   自行车码
    *   广义自行车码
    *   超图积码
    *   量子Margulis码
    *   双变量自行车码
*   **对比方法与译码器：** 所有对比的CSS码均使用**四元置信传播**（BP⁴）译码器进行译码。对于拟对偶CSS码，还额外对比了其专用的CAMEL译码器。
*   **对比指标：** 主要对比不同物理错误率下的逻辑错误率。

### 资源与算力

*   文中**未明确说明**实验所使用的具体算力资源（如GPU型号、数量、训练时长）。仅提到了使用了Gurobi优化器求解混合整数线性规划来计算距离。

### 实验数量与充分性

*   **主要实验：** 论文主要展示了**两组**核心仿真结果，分别对应基于拟对偶码和相交子集码构造的XYZ码。
*   **实验充分性：**
    *   **客观性：** 实验设计较为客观，与多种经典和知名的CSS码进行对比，并尽可能匹配了码长、码率和稳定子权重等关键参数。
    *   **公平性：** 对比相对公平。值得注意的一点是，在对比拟对偶码时，CSS版本的拟对偶码使用了为其专门优化的CAMEL译码器，而XYZ码仅使用了标准BP⁴译码器。即使如此，XYZ码仍表现出性能优势，这反而强化了XYZ构造的潜力。
    *   **充分性：** 对于一篇提出新框架的论文，实验数量基本充分，验证了框架在两种典型构造下的性能优势。然而，缺少消融实验（例如，直接比较去掉Y型稳定子后的CSS版本与XYZ版本的性能差异）是实验设计上的一个不足。此外，实验只涵盖了有限长码的短码长范围。

### 论文的主要结论与发现

*   **非CSS码的可行性与优势：** 研究证实了在有限码长下，放松CSS限制、引入Y型稳定子可以构造出在性能上超越同类型CSS码的量子LDPC码。
*   **理论指导：** 提出的XYZ框架不仅是一个构造工具，还提供了判别“真正”非CSS码的完整代数理论，并给出了分析最小距离的上下界，为理解非CSS码的行为提供了理论支撑。
*   **构造实例：** XYZ²六边形码被证明是此框架的一个特例，表明该框架具有一定的普适性。同时，基于相交子集和拟对偶码的构造展示了框架在生成新型有限长qLDPC码上的实用性。

### 优点

*   **创新性：** 提出了一个结构清晰、代数优雅的非CSS稳定子码构造框架，是对经典CSS框架的直接且重要的推广。
*   **理论深度：** 不仅提出了构造方法，还进行了深入的理论分析，包括“真正”非CSS码的判别准则和最小距离的上下界，理论贡献扎实。
*   **实验验证：** 通过数值仿真验证了所提框架在有限长性能上的优势，实验结果具有说服力。
*   **构造多样性：** 成功地将此框架应用于两类经典的经典码构造中，展示了其广泛的适用性和生成新码的能力。

### 不足与局限

*   **实验规模有限：** 论文仅展示了两组实验，主要在短码长和低码率范围内进行验证，尚未在大规模、高码率的场景下进行探索，对框架的普适性验证不够充分。
*   **消融实验缺失：** 实验设计没有完全隔离Y型稳定子带来的收益。例如，可以将XYZ码与其Y型稳定子被移除后的CSS子码进行直接性能对比，以更清晰地展示Y型校验的贡献。
*   **资源与实现复杂度：** 未提及实验的算力成本，同时，XYZ码的构造要求三个经典码两两正交，在搜索和设计上可能比传统CSS码更复杂。
*   **理论分析复杂性：** 判断一个XYZ码是否能在任意局部泡利重标记下都保持非CSS特性（即局部Clifford等效性）被指明是一个计算复杂性未知的开放问题，这限制了理论工具的实用性。

### 未来工作与开放问题

*   **理论扩展：** 论文指出了判断XYZ码是否在局部Clifford重标记下保持非CSS特性的复杂性，这提示未来可以探索更高效的代数判定算法，或建立与图态、团码等更深层的联系。
*   **构造优化：** 虽然拟对偶和相交子集构造已展示优势，但如何系统性地搜索具有良好距离和稀疏性的三个两两正交经典码仍是一个挑战。未来可借助组合设计、代数编码理论或计算机搜索（如SAT求解器）来生成更多实例。
*   **译码器适配：** 当前仅使用了标准BP⁴译码器，未来可开发专门针对XYZ结构（例如利用Y型校验提供额外软信息）的高效译码算法，有望进一步缩小与理论下界的差距。
*   **长码与高码率场景：** 论文实验局限于短码长（如n=100左右）和较低码率。未来需验证XYZ框架在大规模（n≥1000）、高码率（如R>0.5）量子LDPC码上的性能，并考察码的并行译码收敛性。
*   **容错阈值研究：** 目前仅比较有限长错误率，未进行阈值分析。未来可在退极化信道下对XYZ码族进行阈值估计，比较其与已知CSS码族（如表面码、双变量自行车码）的阈值差异。
*   **噪声模型扩展：** 退极化信道是基准，但实际量子硬件存在偏斜噪声（如更低的Z错误率）。XYZ码的非对称处理能力值得在非对称信道下进行专门评估。

### 论文的整体评分

综合来看，该论文在理论创新、系统性和实验验证方面表现出色，为量子稳定子码的设计开辟了新方向。尽管实验规模和理论完备性存在一定局限，但其主要贡献（统一非CSS构造框架和有限长性能优势）是坚实且有启发性的。按照量子编码领域的一般标准，本文可评为 **A类工作**（具有重要理论意义和实用价值）。

（完）
