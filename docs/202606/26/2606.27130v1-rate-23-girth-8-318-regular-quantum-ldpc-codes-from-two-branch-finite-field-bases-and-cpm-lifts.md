---
title: "Rate-2/3 Girth-8 (3,18)-Regular Quantum LDPC Codes from Two-Branch Finite-Field Bases and CPM Lifts"
title_zh: "来自双分支有限域基和循环置换矩阵提升的率2/3周长为8的(3,18)正则量子LDPC码"
authors: "Koki Okada, Kenta Kasai"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27130v1"
tags: ["query:qec-code"]
score: 9.0
evidence: 构造了围长8的量子LDPC码并进行了解码实验
tldr: "针对量子LDPC码构造中高码率与大围长难以兼得的难题，本文提出基于双分支有限域基和循环置换矩阵提升的方法，得到(3,18)-正则量子LDPC码。该CSS码参数为[[34542,23032]]，H_X和H_Z的Tanner图围长均为8。解码实验表明，在0.01错误率下10^8次试验无失败，相变点接近0.029。该工作为高性能量子LDPC码构造提供了新途径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27130v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1281, \"height\": 517, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27130v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1498, \"height\": 1004, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27130v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 917, \"height\": 1006, \"label\": \"Table\"}]"
motivation: 克服量子LDPC码高码率与大围长难以同时实现的困难，急需新的构造方法。
method: "采用双分支有限域基和循环置换矩阵提升，构建(3,18)-正则量子LDPC码。"
result: "得到码率2/3、参数[[34542,23032]]的CSS码，围长8，解码性能优异。"
conclusion: 本方法成功实现了高码率、大围长且解码性能优秀的量子LDPC码。
---

## 摘要
我们从(3,18)-正则双分支有限域基和度为P=101的循环置换矩阵(CPM)提升构造了一个率为2/3的量子低密度奇偶校验(LDPC)码。得到的码是一个Calderbank-Shor-Steane(CSS)码，参数为[[34542,23032,d≤310]]。我们不将此上界视为真实最小距离的估计；相反，d≤310是目前从结构提升和译码器产生的逻辑错误中获得的最紧上界。该构造的行重为18，列重为3，且H_X和H_X的Tanner图分别具有周长8。使用对数似然比(LLR)联合置信传播(BP)和确定性后处理的译码器实验表明，在p=0.01下10^8次试验中没有失败，并且有限长帧错误率(FER)扫描估计在p=0.029附近发生转变。

## Abstract
We construct a rate-$2/3$ quantum low-density parity-check (LDPC) code from a $(3,18)$-regular two-branch finite-field base and a circulant-permutation-matrix (CPM) lift of degree $P=101$. The resulting code is a Calderbank-Shor-Steane (CSS) code with parameters $[[34542,23032,d\le 310]]$. We do not regard this upper bound as an estimate of the true minimum distance; rather, $d\le310$ is the tightest upper bound currently obtained from structural lifts and decoder-produced logical errors. The construction has row weight 18 and column weight 3, and the Tanner graphs of $H_X$ and $H_Z$ separately have girth 8. Decoder experiments with log-likelihood-ratio (LLR) joint belief propagation (BP) and deterministic post-processing show no failures in $10^8$ trials at $p=0.01$, and a finite-length frame error rate (FER) sweep estimates the transition near $p=0.029$.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在量子纠错中，构造高码率（接近2/3）、大围长（girth 8）、且满足CSS正交条件的正则量子LDPC码。传统的量子LDPC码构造需要在稀疏性、码率、短环控制和正交性之间取得平衡，而现有构造往往在有限长码中难以同时实现高码率和大围长。
- **背景**：经典LDPC码已广泛应用于通信；在量子领域，CSS码通过两个二进制奇偶校验矩阵构造，但要求它们严格正交。已有的超图乘积码、平衡乘积码、提升乘积码等提供了渐近正码率和距离的族，但有限长设计仍需显式选择度数分布、正交约束、提升结构和译码器。高码率对容错量子计算至关重要，可降低物理量子比特开销。
- **研究动机**：本文旨在通过代数构造方法，得到一个有限长的、性能可验证的高码率量子LDPC码实例，并通过译码实验确认其可行性，为实用化量子纠错提供候选码。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用“双分支有限域基”+“循环置换矩阵（CPM）提升”的两步构造法。基矩阵在有限域F_19上定义，通过代数条件保证正则性、无同侧4环和CSS正交性；CPM提升用于增加码长，同时保持基的约束，并且通过仿射线性移位参数化，将提升后的约束转化为线性方程组。
- **关键技术细节**：
    1. **基构造**：使用域F_19的9阶乘法子群M，基列索引为(b,t,h)，b∈{0,1}（分支），t∈F_19，h∈M。基行索引为(g,r)，g∈{0,1,2}，r∈F_19。X侧和Z侧分别使用系数矩阵A和B，通过方程r = t + A_{b,g}h (mod 19)定义边。列重为3，行重为18。
    2. **正交条件**：通过商-陪集测试确保同侧无4环（Δ₀/Δ₁∉M）和跨侧偶数交（Δ₀/Δ₁∈M），从而保证H_X·H_Z^T = 0。
    3. **CPM提升**：用度P=101的循环置换矩阵替换基中每个1。移位量采用仿射线性形式σ_S(g,b,h;t)=c_{S,g,b,h}+d_{S,g,b,h}t (mod 101)。提升后CSS正交性变为线性方程组（1539个方程，秩167，自由变量49个）。同侧6环的闭合同样变为线性形式，通过随机搜索在解空间中排除所有使线性形式为零的点。
    4. **低权重逻辑支持排除**：通过迭代构造过程，先构建一个CPM提升并用译码器测试，将观测到的低权重逻辑失败投影到基列，增加约束，重新搜索提升系数，直到所有已知的低权重逻辑支持（共285个）被排除。
    5. **最终码**：得到码长n=34542，k=23032，码率≈0.6668，行重18，列重3，H_X和H_Z的Tanner图围长均为8。距离上界d≤310（通过全变量综合征求解得到）。

### 3. 实验设计：使用的数据集/场景、基准、对比方法

- **数据集/场景**：没有外部数据集。实验是在模拟的量子误码信道（比特翻转/相位翻转错误）下进行的。物理错误概率p设置为不同值（0.01，以及从0.034向下扫描的多个点）。
- **基准**：论文自身并未与其他量子LDPC码（如表面码、Hypergraph产品码等）进行直接性能对比。它只将自己的FER曲线与BP阈值（p=0.034）和码率2/3的Hashing界（p=0.04454）进行了比较。
- **对比方法**：没有对比其他译码器或纠错码。论文主要评估自身构造的码在LLR联合BP译码器加确定性后处理下的性能。

### 4. 资源与算力

- **明确说明**：论文中没有提及任何GPU型号、数量、训练时长或专用计算集群。整个构造和译码实验是在常规CPU上完成的（例如，译码仿真、线性方程组求解、有限域运算）。算力需求不大。
- **注意**：由于是纯代数构造和译码仿真，不需要大规模训练，因此算力不是重点。

### 5. 实验数量与充分性

- **实验数量**：
    - 主要实验1：在p=0.01下进行了10^8次试验，零成功率（无失败）。这是非常有说服力的低FER证据。
    - 实验2：从p=0.034向下扫描的FER曲线，给出了多个数据点（如p=0.0294时FER≈5.14×10^{-3}，p=0.0285时FER≈3.94×10^{-4}）。
    - 附加分析：对p=0.0237测量中的18个失败实例进行了详细分析（ETS库修复、全变量求解等）。
- **充分性与公平性**：
    - **充分性**：主要性能验证是充分的，特别是10^8试验证明在p=0.01下极低FER。FER扫描覆盖了从高到低的多个点，可以观察到相变。
    - **限制**：实验仅针对这一个具体的码（P=101，F_19），没有评估不同参数（不同P、不同有限域）下的码，也没有与其他构造方法进行性能对比。因此，无法判断该码在同类码中的相对优劣。此外，译码器后处理包含手动设计的ETS库，其通用性可能有限。

### 6. 论文的主要结论与发现

1. 成功构造了一个有限长、高码率（2/3）、正则(3,18)、围长8的量子CSS LDPC码，参数为[[34542,23032,d≤310]]。
2. 在物理错误率p=0.01下，10^8次译码试验零失败，经验FER上界约为3×10^{-8}。
3. FER扫描估计出相变点在p≈0.029附近，接近Hashing界。
4. 通过全变量综合征求解，得到了一个具体的重量310的逻辑算子，从而给出了距离上界d≤310，比单纯的结构提升界（606）更紧。
5. 验证了通过迭代构造排除低权重逻辑支持的有效性。

### 7. 优点

- **构造的代数性**：基和提升均基于有限域代数，约束条件转化为线性方程和线性形式，使得搜索空间可处理，并且可以严格验证正交性和围长。
- **优异的译码性能**：在高码率下，译码阈值接近理论极限，且错误平层极低（p=0.01下无错误）。
- **系统性的低权重逻辑排除**：利用译码实验反馈来迭代加强构造，是一种实用且有效的方法。
- **显式距离上界**：通过实际求解得到了非平凡的上界，比单纯理论估计更精确。
- **论文结构清晰**：方法、构造步骤、实验设计均明确说明，可重复性较好。

### 8. 不足与局限

- **距离下界缺失**：论文明确表示“没有证明d≥20”，仅给出了上界。因此该码的真实最小距离未知，可能远小于310。
- **缺乏对比基准**：未与其他现有量子LDPC码（如表面码、平衡乘积码、相同码率下的其他构造）进行性能比较，使结果缺乏参照。
- **仅一个实例**：只展示了一个特定参数（P=101, F_19）的码，泛化能力和参数敏感性未探讨。
- **译码器特殊性**：后处理中的ETS库是针对该码特定故障模式手动设计的，是否可移植到其他码或通用解码器不明确。
- **实验覆盖有限**：只有FER曲线，没有给出逻辑错误率与码距的关系，也没有进行不同迭代次数、不同译码算法的对比。
- **物理实现未涉及**：该构造是否适用于当前或近期量子硬件（如超导、离子阱、中性原子阵列）未讨论，常见的实现约束（如连接性、并行读取）未被考虑。
- **算力资源未披露**：虽然不影响结论，但缺失了实验耗时信息，不利于复现。

（完）
