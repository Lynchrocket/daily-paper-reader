---
title: Quantum Codes with Transversal $CCZ$ Gates and Sublinear $Z$-Stabilizers
title_zh: 具有横向$CCZ$门和次线性$Z$-稳定子的量子码
authors: "Ohad Elishco, Itzhak Tamo"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22472v1"
tags: ["query:qec-code"]
score: 9.0
evidence: 构建具有横向CCZ门和次线性Z稳定器的CSS码
tldr: "实现横向CCZ门是量子纠错码的重要目标，但现有构造受限于次线性维度。本文提出精细化穿刺定理，仅对选定穿刺集施加条件，并结合代数扩展器码，构造了具有横向CCZ门的CSS码，参数为[[N, Θ(N), Ω(N^{1/m})]]，Z-稳定子生成集权重O(N^{1/m})。进一步通过射影多重性方法将字母表降至固定素域，保持横向CCZ门和次线性Z-稳定子局部性，距离接近n^{1/m}。该方法为容错量子计算提供了新途径。"
source: arxiv
selection_source: fresh_fetch
motivation: 克服现有构造中因小对偶距离导致的次线性维度限制，实现具有横向CCZ门和低权重Z-稳定子的高性能量子CSS码。
method: 提出精细化穿刺定理，用局部条件替换全局对偶距离假设；结合代数扩展器码与射影多重性乘法友好码。
result: "构造了[[N, Θ(N), Ω(N^{1/m})]]的CSS码，具横向CCZ门；Z-稳定子生成集权重O(N^{1/m})；固定素域版本距离达n^{1/m}（多对数因子内）。"
conclusion: 展示了系统构造具横向CCZ门与次线性Z-稳定子权重量子码的方法，为容错量子计算提供高效资源。
---

## 摘要
我们构造了量子CSS码，具有横向\(CCZ\)门，其\(Z\)-稳定子允许次线性权重的生成集。我们基于Guruswami和Golowich \cite{GG24}的代数穿孔框架，该框架将具有所需Schur积和距离条件的经典码转化为具有横向\(CCZ\)的CSS码。然而，将该框架直接应用于\cite{KT26}的代数扩展码会遇到其对偶距离小的问题，因此只能产生次线性的量子维度。我们的关键技术步骤是一个精细化的穿孔定理，其中全局对偶距离假设被替换为仅对所选穿孔集的条件。将该定理应用于代数扩展码，对于每个固定的\(m\geq 3\)，给出了显式的增长字母表CSS码，参数为\([[N,Θ(N),Ω(N^{1/m})]]\)，并且具有横向\(CCZ\)门。此外，\(Z\)-稳定子空间具有权重为\(O(N^{1/m})\)的显式生成集。我们还利用乘法友好码的射影多重性版本将字母表简化为固定素域。由此产生的固定素域CSS码三元组，长度为\(n\)，仍然具有横向\(CCZ\)门。其维度接近线性，距离为\(n^{1/m}\)（至多对数因子），并且\(Z\)-稳定子局部性仍然是次线性的，同样至多对数损失。

## Abstract
We construct quantum CSS codes with transversal \(CCZ\) gates whose \(Z\)-stabilizers admit sublinear-weight generating sets. We build on the algebraic puncturing framework of Guruswami and Golowich \cite{GG24}, which turns classical codes with the required Schur-product and distance conditions into CSS codes with transversal \(CCZ\). However, applying the framework directly to the algebraic expander codes of \cite{KT26} runs into their small dual distance, and therefore produces only sublinear quantum dimension. Our main technical step is a refined puncturing theorem in which the global dual-distance assumption is replaced by a condition only on the selected puncturing set. Applying this theorem to algebraic expander codes gives explicit growing-alphabet CSS codes with parameters \([[N,Θ(N),Ω(N^{1/m})]]\), for every fixed \(m\geq 3\), and with transversal \(CCZ\) gates. Moreover, the \(Z\)-stabilizer space has an explicit generating set of weight \(O(N^{1/m})\). We also reduce the alphabet to a fixed prime field using a projective-multiplicity version of multiplication-friendly codes. The resulting fixed-prime-field CSS code triples, of length \(n\), still have transversal \(CCZ\) gates. Their dimension is near-linear, their distance is \(n^{1/m}\)   up to polylogarithmic factors, and the \(Z\)-stabilizer locality remains sublinear, again up to polylogarithmic losses.

---

## 论文详细总结（自动生成）

好的，以下是对论文《具有横向`CCZ`门和次线性`Z`稳定子的量子码》的详细中文总结。

### 论文核心问题与整体含义

- **研究动机:** 实现容错量子计算的关键一步是构建支持通用门集的量子纠错码。横向门（Transversal gates）能天然防止错误传播，但Eastin-Knill定理指出仅靠横向门无法构成通用门集。`CCZ`门作为Clifford层次中的非Clifford门，与Clifford门结合可实现通用性。因此，构造同时具备良好编码参数（如线性维度、多项式距离）和**低重量稳定子**（便于容错测量）的、支持横向`CCZ`门的量子码是一个重要目标。
- **核心问题:** 现有基于代数穿孔框架和代数扩展器码的构造，虽然能实现横向`CCZ`门，但由于代数扩展器码的**对偶距离太小**（仅为`Θ(N^{1/m})`），直接应用会导致构造出的量子码维度**仅为次线性**（`O(N^{1/m})`），无法实现高码率。同时，如何在该框架内获得低权重的稳定子生成集也是一个未解决的问题。
- **整体含义:** 本文通过精细化的理论分析，证明了对偶距离小并非代数扩展器码的根本缺陷，只要选择合适的穿孔集，仍能构造出**线性维度、多项式距离、支持横向`CCZ`门，且`Z`稳定子生成集权重为次线性**的量子CSS码。这项工作为在代数框架内实现高性能、低开销的容错量子计算开辟了新路径。

### 论文提出的方法论

- **核心思想:** 克服直接应用Guruswami-Golowich（GG）穿孔定理带来的维度损失。GG定理要求穿孔集`A`的大小小于全局对偶距离`d(C⊥)`，这限制了`A`的大小。本文的**精细化穿孔定理**用一个**依赖于`A`的距离条件**`d_A(C⊥)`（衡量对偶码字在`A`外部的支持大小）替换了全局条件`d(C⊥)`。通过精心设计`A`，可以使其大小远超`d(C⊥)`，但仍能保证`d_A(C⊥)`足够大。
- **关键技术细节:**
    1.  **精细化穿孔定理 (Theorem 3.1):** 对于代码`C`和集合`A`，在满足三个简单条件（即`C|A`是满射、`C ∩ FA`和`C*³ ∩ FA`为零空间）后，可以构建一个CSS码，其距离由`d_A(C)`和`d_A(C⊥)`中的较小者决定。这个定理解除了对全局对偶距离`d(C⊥)`的依赖。
    2.  **选择穿孔集`A`:** 构造一个**低码率**的代数扩展器码`C_{r0}`（r0 < r），并选择`A`为`C_{r0}`的一个**插值集**。由于`C_{r0}`的码率低，其插值集`A`可以很大（线性大小`Θ(N)`）。关键命题(Proposition 4.4)证明，对于高码率码`C_r`的任意非零对偶码字`z`，其在`A`外部的支持大小至少为`Ω(N^{1/m})`，从而保证了`d_A(C_r⊥)`的规模。
    3.  **`Z`-稳定子的低权重生成:** 利用`A`是低码率码`C_{r0}`的插值集这一特性，可以证明`A`在每个局部轨道上非常稀疏。这使得`C⊥_r`被缩短到`A`外部后，其生成集可以限制在**单个轨道**（重量`O(N^{1/m})`）或**两个轨道的并集**（重量`O(N^{1/m})`）上，从而得到次线性的`Z`-稳定子生成权重。
    4.  **字母表缩减:** 为了将量子码定义在固定素域（如`F_p`）上，论文采用了GG框架的缩减方法，但内部乘法友好量子码使用了一种**射影多重性**（projective-multiplicity）版本的构造。这允许在任意有限域（包括特征2）上工作，并能在码长`n`增加时，严格追踪`Z`-稳定子权重，确保其增长可控。

- **算法流程（文字说明）:**
    1.  选定参数`m≥3`和码率参数`r0 < r < 1/3`。
    2.  构建代数扩展器码`C_r`和`C_{r0}`。
    3.  为`C_{r0}`构造一个大小为`Θ(N)`的插值集`A`。
    4.  应用精细化穿孔定理，以`C_r`为原始码，`A`为穿刺集，构造出具有线性维度、多项式距离和横向`CCZ`门的量子CSS码。
    5.  利用代数扩展器码的Tanner结构，证明其`Z`-稳定子空间由`O(N^{1/m})`重量的生成元生成。
    6.  使用射影多重性乘法友好内码，将上述码通过代数缩减框架转换为固定素域上的量子码，并确保其参数和`Z`-稳定子特性得到保留。

### 实验设计

- **数据与场景:** 本文是纯理论工作，不涉及传统意义上的数值实验或数据集。其“实验”是数学构造和性能分析。
- **Benchmark:** 论文的主要对比基准是 **Guruswami和Golowich (GG) 的原始穿孔定理**。直接应用该定理于代数扩展器码会得到次线性维度，这是本文试图解决的“病灶”。
- **对比方法:**
    - 与`[GG24]`（Guruswami-Golowich）的结果对比，突出维度参数的巨大飞跃（从`O(N^{1/m})`到`Θ(N)`）。
    - 与`[GL25, GG25b, Zhu25]`等近期工作的结果对比，指出虽然这些工作在某些参数（如稳定子权重）上可能更优，但本文在代数框架内实现了稳定的进展。

### 资源与算力

- 文中**未提及**任何与计算资源、GPU型号、训练时长相关的信息。论文属于理论数学和编码理论范畴，核心贡献在于提出新的数学框架和证明。

### 实验数量与充分性

- **实验数量:** 无数值实验。
- **充分性与客观性:**
    - **理论证明的充分性:** 论文的结论全部基于严格的数学证明。核心的精细化穿孔定理、插值集的性质、稳定子权重的分析、以及字母表缩减的追踪，每一步都给出了完备的论证。
    - **结构完整性:** 论文结构清晰，包含所有所需引理和定理的证明。对于“射影多重性”内码的构造，也专门提供了一个附录进行详细说明。
    - **局限性:** 没有通过数值模拟来展示参数的实际表现或进行公平性对比，这在纯粹理论工作中是常见的，但其结果的实际可行性和优势依赖于未来编译或实现层面的验证。

### 论文的主要结论与发现

- **主要结论:** 可以构造出具备如下优越特性的量子CSS码族：
    1.  **渐近好码:** 参数为`[[N, Θ(N), Ω(N^{1/m})]]`，即在块长`N`下，具有**线性维度**（高码率）和**多项式距离**（高容错能力）。
    2.  **横向`CCZ`门:** 支持关键的非Clifford门，与Clifford门结合可实现通用量子计算。
    3.  **次线性`Z`-稳定子:** `Z`-稳定子空间具有显式的**次线性权重**生成集（`O(N^{1/m})`），这极大地便利了容错的错误症状提取。
- **发现:**
    - 代数扩展器码小的对偶距离**并非**实现高码率量子码的根本性技术障碍。对偶距离的“窟窿”可以通过选择恰当的穿刺集来弥补。
    - 代数穿孔框架可以自然地与**低权重稳定子**兼容，至少对于一侧的稳定子是如此。
    - 通过射影多重性内码，可以将字母表从增长字母表降低到固定素域，同时保持上述优良特性，仅伴随对数量级的损失。

### 优点

- **方法论创新:** 提出的精细化解耦定理（Refined Puncturing Theorem）是关键的创新点，它放宽了经典穿孔定理中过于严格的条件，为在低对偶距离场景下获得高维度量子码提供了理论基础。
- **参数突破:** 首次在代数穿孔框架内，实现了**同时具有线性维度、多项式距离、横向`CCZ`门和次线性重量`Z`-稳定子**的量子码。相比现有工作，在维度上取得了指数级的提升（从次线性到线性）。
- **构造的显式性:** 整个构造过程（包括插值集`A`的选取、稳定子生成元的形成）都是显式的，适合实际实现。
- **框架的普适性:** 论文提出的精细化穿孔定理和利用低码率码插值集的思想，可能对其它基于代数线性码的量子码构造具有启发性。

### 不足与局限

- **单侧局部性:** 目前构造仅实现了`Z`-稳定子一侧的次线性权重，而`X`-稳定子的权重仍然是**线性**的（`Θ(N)`）。这导致了码的**非对称性**，在实际系统中可能仍是瓶颈。
- **缺少译码算法:** 论文没有为所构造的量子码提供有效的译码算法。一个实用的量子纠错码需要有高效的译码器。
- **应用场景限制:** 虽然理论参数好，但码是“几乎好”但不是“渐近好”（指距离和维度都线性）。且`Z`稳定子重量是`O(N^{1/m})`，对于小的`m`（如`m=3`），其指数`1/3`在`N`很大时仍然可观。论文没有讨论这些参数在大尺度的实际系统（如容错阈值）中的具体表现。
- **固定字母表版本的参数损失:** 在从增长字母表降低到固定素域时，维度从`Θ(N)`变为`Ω(N/(log N)^4)`，距离从`Ω(N^{1/m})`变为`Ω(N^{1/m}/(log N)^{4/m})`，稳定子权重从`O(N^{1/m})`变为`O(N^{1/m}(log N)^{1-4/m})`，引入了多对数级别的损失。

（完）
