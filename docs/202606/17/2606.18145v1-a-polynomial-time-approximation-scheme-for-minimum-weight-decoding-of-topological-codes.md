---
title: A polynomial-time approximation scheme for minimum-weight decoding of topological codes
title_zh: 拓扑码最小权重解码的多项式时间近似方案
authors: "Shouzhen Gu, Lily Wang, Aleksander Kubica"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.18145v1"
tags: ["query:qec-decoding"]
score: 9.0
evidence: 拓扑码最小权重解码的多项式时间近似方案
tldr: "二维拓扑平移不变稳定子码的最小权重解码已被证明是NP难问题，但本文证明其仍存在多项式时间近似方案（PTAS）。该方案基于Arora的欧几里得问题PTAS，将解码转化为点激发由弦误差连接的形式。对于任意常数ε>0，可在多项式时间内找到权重在最优值1+ε倍以内的恢复算子。该结果突破了NP难限制，并推广到更高维拓扑码和带噪声的量子存储器。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18145v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1275, \"height\": 1174, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18145v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 708, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18145v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 868, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18145v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1612, \"height\": 307, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18145v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1340, \"height\": 690, \"label\": \"Figure\"}]"
motivation: 二维拓扑码的最小权重解码被证明是NP难的，但实际应用中需要高效近似解，因此探索PTAS成为关键。
method: 借鉴Arora的欧几里得问题PTAS，将解码重构为点激发与弦误差连接问题，设计多项式时间近似算法。
result: "对任意ε>0，能在多项式时间内输出权重不超过最优值1+ε倍的恢复算子。"
conclusion: 证明2D TTI码的最小权重解码存在PTAS，且可扩展至高维拓扑码和带噪声的量子存储器。
---

## 摘要
二维拓扑平移不变（2D TTI）稳定子码是容错量子计算的核心，但使用它们需要解决解码问题。最近研究表明，即使在基本设置下，如Pauli $Z$错误的颜色码和Pauli $X$、$Y$、$Z$错误的环面码，这些码的最小权重解码也是NP难的。然而，我们证明2D TTI码的最小权重解码仍然允许多项式时间近似方案（PTAS），即对于任意常数$\varepsilon>0$，可以在多项式时间内找到权重在最小值的$1+\varepsilon$乘法因子内的恢复算子。我们的方法基于Arora针对欧几里得问题（如旅行商问题）的PTAS，并适用于解码可以表示为由弦状错误连接的类点激发的情况。因此，它超越了二维，涵盖了某些高维拓扑码和量子存储器，包括具有现象级或电路级噪声的环面码。

## Abstract
Two-dimensional topological translationally invariant (2D TTI) stabilizer codes lie at the heart of fault-tolerant quantum computation, but using them requires solving the decoding problem. Minimum-weight decoding of these codes was recently shown to be NP-hard, even in basic settings, such as the color code with Pauli $Z$ errors and the toric code with Pauli $X$, $Y$ and $Z$ errors. Here, we prove that minimum-weight decoding of 2D TTI codes nonetheless admits a polynomial-time approximation scheme (PTAS), i.e., for any constant $\varepsilon>0$, a recovery operator of weight within a multiplicative factor of $1+\varepsilon$ of the minimum can be found in polynomial time. Our approach builds on Arora's PTAS for Euclidean problems, such as the traveling salesman problem, and applies when decoding can be cast in terms of point-like excitations connected by string-like errors. It therefore extends beyond two dimensions, covering certain higher-dimensional topological codes and quantum memories, including the toric code with phenomenological or circuit-level noise.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：二维拓扑平移不变（2D TTI）稳定子码（如表面码、颜色码）的**最小权重解码**问题——给定错误综合征，寻找权重最小的恢复算子。近期工作证明该问题在基本设置下是**NP难的**（如颜色码只含Pauli Z错误、环面码含所有Pauli错误）。
- **研究动机**：虽然精确求解是NP难的，但实际容错量子计算中，我们更关心能否**高效地找到近似最优解**（权重接近最小）。因此，探索是否存在多项式时间近似方案（PTAS）具有重要意义。
- **整体含义**：本文首次证明，对于任意2D TTI码和任意常数ε>0，可以在多项式时间内找到权重不超过最优值(1+ε)倍的恢复算子，从而绕开了NP难度的障碍，为实际解码提供了理论保障。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将解码问题类比为**欧几里得组合优化问题**（如旅行商问题），借助Arora的PTAS框架——递归划分网格、使用portal、rerouting和patching技巧，将任意错误变形为“r-light”错误（每条线段上只经过portal且最多r次），再通过动态规划找到最优r-light错误。
- **关键技术细节**：
    - **递归划分**：将L×L晶格递归划分为四级正方形（level-0到level-i0），每个正方形边界上均匀设置portal（数量m=O((1/ε)log L)），每个portal允许支持最大r=O(1/ε)次错误权重。
    - **Patching Property**（修补性质）：对于任意一条线段上的错误，存在一个等价的错误，其支持仅位于该线段的缓冲区内，且与线段本身的交点数有常数上界。在一般2D TTI码中，该性质在移除了所有紧邻边界plaquette上的激发后成立。
    - **Rerouting Property**（重路由性质）：对于任意线段上的错误，可以重路由到最近的portal，使得错误与线段的交点仅出现在portal上，且权重增加因子受控。
    - **动态规划（Algorithm 1）**：自底向上递归求解。对于每个正方形和边界上的固定portal错误配置，搜索所有可能的“lift”（内层portal配置），组合内层子正方形的解，得到当前正方形的最小权重r-light解。底层正方形（大小为s0）通过穷举（常数时间）求解。
    - **随机化移位**：为了以高概率得到(1+ε)近似，算法对划分网格的偏移量（a,b,c,d）进行随机选取，确保在期望意义下各条线段被处理的次数与最优解权重成比例。

## 3. 实验设计

- **实验场景**：本文为**纯理论证明论文**，不包含传统数值实验（无需数据集或benchmark对比）。但论文中讨论了**多个具体应用场景**：
    - 2D环面码（Pauli X、Y、Z错误）
    - 2D颜色码（Pauli Z错误）
    - 推广到更高维拓扑码（3D子系统环面码、颜色码）
    - 量子存储器（(2+1)D环面码 + 现象级/电路级噪声）
- **对比方法**：文中未进行实验对比，但提到了之前的最佳近似比为2（通过独立匹配X和Z激发），以及启发式解码器。本文的PTAS理论上比这些方法有更优的近似比（任意(1+ε)）。
- **Benchmark**：理论基准是最优最小权重解；论文证明算法能找到权重不超过(1+ε)倍最优值的解。

## 4. 资源与算力

- **文中未提及任何具体的GPU型号、数量或训练时长**。算法复杂度为理论分析：时间复杂度为 \(L^2 (\log L)^{O(1/\varepsilon)}\)，空间复杂度相同（对于去随机化版本，时间变为 \(L^4 (\log L)^{O(1/\varepsilon)}\)）。由于没有实际运行，无算力需求报告。

## 5. 实验数量与充分性

- **实验数量**：零。论文完全通过**数学证明**建立正确性与复杂度，未进行任何数值仿真或消融实验。
- **充分性**：证明结构完整，从帕匹配/重路由性质的构造，到动态规划算法正确性，再到随机化下期望近似比的分析，逻辑链严密。但缺少数值验证，因此**在实际解码阈值、对有限尺寸码的性能等方面未提供实验证据**。对于实际应用而言，理论保证充分，但实验验证不足。

## 6. 论文的主要结论与发现

- **主要结论**：对于任意2D TTI稳定子码，最小权重解码存在多项式时间近似方案（PTAS）。具体来说，对于任意常数ε>0，算法可以在 \(L^2 (\log L)^{O(1/\varepsilon)}\) 时间内输出一个恢复算子，其权重不超过最优解权重的 (1+ε) 倍。
- **发现**：
    - 证明了patching和rerouting性质在一般2D TTI码中成立，前提是移除了靠近划分边界的激发。
    - 该PTAS不仅适用于2D码，还能扩展到某些高维拓扑码和带测量噪声的量子存储器（如(2+1)D环面码），只要码具有“点激发+弦错误”结构。
    - 附加推论：该解码器可为2D颜色码纠正权重上至 (1-ε)d/2 的错误（d为距离），逼近最优半距离界限。
- **理论意义**：突破了最小权重解码NP难的障碍，首次给出该类问题的PTAS。

## 7. 优点

- **理论突破**：首次证明2D TTI码最小权重解码存在PTAS，解决了该领域一个重要的开放问题。
- **方法通用**：基于Arora框架，不依赖码的特殊结构，仅需点激发+弦错误结构，因此可推广到更高维和带噪声的量子存储器。
- **证明严谨**：给出了详细的动态规划算法、patching和rerouting性质的构造性证明，以及随机化下期望近似比的分析。
- **实际价值**：为设计高效解码器提供了理论基石，有望与现有匹配解码器等结合，提升实际性能（虽然文中未做实验）。
- **提供去随机化版本**：保证100%成功率的版本（仅增加多项式时间因子），增加了实用性。

## 8. 不足与局限

- **缺乏数值验证**：纯理论论文，没有仿真实验来展示实际性能（如阈值、有限尺寸下的近似比、与现有解码器的对比）。
- **时间复杂度指数依赖于1/ε**：虽然对固定ε是多项式时间，但指数项为 \(O(1/\varepsilon)\) 出现在对数幂中，实际中ε很小时可能依然较慢。
- **实现复杂度高**：算法涉及动态规划、递归划分、portal配置查找，工程实现可能复杂，尤其是在真实量子硬件条件下（需处理电路级噪声的完整解码图）。
- **通用性依然受限**：对于高维一般拓扑码，patching和rerouting性质需要特殊条件（如错误不能缠绕在面上），文中只验证了特定场景（如(2+1)D环面码），未给出对所有高维码的通用证明。
- **只保证近似比，不保证逻辑错误率**：最小权重解码近似并不直接等价于最优逻辑错误率，因此PTAS解码器的实际容错阈值可能存在隐藏的退化风险。
- **未考虑退化码效应**：许多拓扑码具有退化性（等价格逻辑算子导致不同权重错误等价），算法基于最小权重错误，可能忽略等权重等效错误，影响最优性。不过近似比仍对最小权重成立。

（完）
